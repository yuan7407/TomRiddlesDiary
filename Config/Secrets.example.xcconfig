// Secrets.example.xcconfig — 模板（可提交）。
//
// ── 怎么用 ──
// 1. 把本文件复制成同目录的 `Secrets.xcconfig`
// 2. 在那个副本里填真实的 key
// 3. 完事。`Secrets.xcconfig` 已被 .gitignore 挡住，永远不会提交
//
//    cp Config/Secrets.example.xcconfig Config/Secrets.xcconfig
//
// 端点、路径、模型名的默认值在同目录的 `Oracle.xcconfig`（那个文件可提交、不含密钥）。
// 本文件里的任何一行都会**覆盖**那边的同名值，所以要换模型或换供应商，
// 在这里加一行就行，不用改可提交的文件。
//
// ── 安全边界（`AGENTS.md` 红线，不是建议）──
// · 这个 key 只挂在 **Debug** 配置上。Release 配置读不到它，所以分发出去的包里
//   **根本不存在**这个 key。
// · 但本地 Debug 包仍然是可提取的——App 就是个 zip，字符串能 strings 出来。
//   所以**含 key 的 build 永不分发**，不上 TestFlight，不发给任何人。
// · 请在供应商后台给这个 key 设**额度上限**，并且只开你要用的那一个模型的权限。
//   万一真泄了，损失有上限。
// · 正式分发必须换成安全后端（计划 G）：key 留在服务端，客户端只调自己的后端。
//
// ── 2026-09-01 变了变量名 ──
// 旧模板里是 `MODEL_API_KEY`，更早还有 `DASHSCOPE_API_KEY` / `GEMINI_API_KEY` /
// `FLUX_API_KEY`。前两个是换供应商前的遗留，后两个是图像生成那条腿（已砍掉）。
// 现在统一叫 `ORACLE_*`，和代码里读的名字一致。
// **如果你的 Secrets.xcconfig 还是旧版本，里面的 key 不会被读到**——变量名对不上。

// 你的 API key。DeepSeek 的形如 sk- 开头。
ORACLE_API_KEY = sk-在这里粘贴你的真实key

// 下面两行只在你要覆盖默认值时才需要（默认值见 Oracle.xcconfig）。
// 换供应商时才改。注意 xcconfig 里 `//` 是注释符，所以 host 不要带 https:// 前缀。
// ORACLE_HOST = api.deepseek.com
// ORACLE_MODEL = deepseek-v4-flash
