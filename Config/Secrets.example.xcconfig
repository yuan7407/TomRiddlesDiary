// Secrets.example.xcconfig — 模板（可提交）。
// 用法：复制本文件为同目录 Secrets.xcconfig，填入真实值。
// Secrets.xcconfig 已在 .gitignore：绝不提交。
// 永久 key 只允许用于本地、非分发 DEBUG 实验；含永久 key 的 build 绝不分发。
// TestFlight/生产环境必须经安全后端，永久 key 不得进入客户端二进制或可提取流量。
//
// 2026-08-26：图像生成那条腿已砍掉（回应改为纯手写文字），因此删除原有的
// GEMINI_API_KEY / FLUX_API_KEY 两个图像模型占位——它们已无对应功能。
//
// 现状：本文件尚未接进 Xcode 工程（没有任何 baseConfigurationReference 指向它），
// 所以填了值代码也读不到。接线安排在后端就绪时（计划 G3），届时这里应该放的是
// 「自建后端地址」而不是模型永久 Key——客户端不该持有模型 Key。
//
// 文本模型 Key（仅本地非分发 DEBUG 实验用；正式调用必须经自建后端）
// 区域、Workspace 与端点必须按实际账号页面复核，不得凭记忆填写。
MODEL_API_KEY = your_local_debug_key_here
