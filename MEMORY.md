<!-- 动态状态与决策。会话开始或用户说“继续”时先读本文件。规则见 AGENTS.md，项目说明与计划见 README.md。保持 ≤120 行。
     决策编号**不重新编号**：代码注释引用了决策 14/17/18/20。删行时留空号，合并时并列写号。 -->
# 项目记忆

## 当前状态

- **阶段**：输入端整条链路已通（E3a→E3b→E3c→E3d→E4），输出端逐笔生长已通（E1 + E2；E8 字体脚手架已于 2026-09-01 随 H9 删除），回应落点 E9a–E9e 全部完成。计划编号见 `README.md` 的 A–H 表；字母是名字，执行先后见那里的「执行顺序」。
- **链路上唯一缺的一环是 Oracle（E6）**：写字 → 停笔 → 成页 → 识别出文字 → **断在这里**。文字拿到了，没有东西能把它变成回应。接上之后 `DiaryPageModel.beginReply` 就是入口，后面的定落点、排笔画、手绘化、逐笔重播、落笔中断全部就绪。`DiaryPageView` 的 Xcode 预览里这条链路（含落点）已经能真正跑起来，用的是写死的假文字，所以刻意不进 App。
- **App 跑起来的真实样子**：一张能写字的纸，零按钮；停笔后成页并识别；**DEBUG 里魂会用写死的假文字真的回你**（挨着你刚写的那句话，逐笔长出来）；Release 里纸上如实说魂还没接上。DEBUG 构建会往控制台打 A10 校准报告与识别结果，release 里这两段代码不存在。
- **引擎**：`StrokePipeline → StrokeHumanizer → StrokeReplayTimeline`，入口只吃 `[Polyline]`。两个真实生产者都已就位：`PencilStrokeReader`（用户手写）与 `GlyphStrokeLayout`（字形笔顺）。渲染层是 `Features/Response/HandwritingReplayView`，入参 `ReplayPlayback` 三态，坐标 1:1。
- **单位契约（D1/D2）**：坐标一律页面点；**只有字高用毫米**（默认 9 mm ≈ 46.8 pt），其余全是字高的比例。`Configuration/` 下四个文件是经验性常量的唯一定义处：`PageMetrics`（唯一毫米↔点换算）、`HandwritingFeel`、`PageAppearance`、`InteractionSettings`。引擎已无默认参数。
- **验证**：282 个 XCTest 在 iOS 26.5 与 27.0 两个模拟器上都通过（26 上 4 条识别用例如实跳过）。测试必须加 `-parallel-testing-enabled NO`，构建须用 Xcode 27（`DEVELOPER_DIR` 指定，不改全局 `xcode-select`）。
- **未接入**：Oracle、任何网络或密钥、加密存储、后端、TestFlight。
- **目录**：`Sources/` 下 `Configuration / Features(Canvas·Response) / Handwriting / StrokeEngine / Oracle / Calibration / Resources` + 入口；根目录另有 `C1-response-quality-set.md`（模型评测数据，不是第四份文档）。

## 决策表

| # | 决策 | 结论 | 为什么 |
|---|---|---|---|
| 1 | 核心交互 | 手写文字，不做文字聊天复制品；涂鸦识别往后排 | 笔画与逐笔节奏是差异化内核 |
| 2 | 手感来源 | 本地引擎负责，不靠模型输出最终笔画 | 模型笔画不稳定，节奏与压感必须可控 |
| 3 | 笔画源 | 引擎只吃有序向量 `[Polyline]` | 位图抽骨架实机效果差且已删。两个真实生产者已就位（PencilKit 手写、字形笔顺中线） |
| 4+5 | 模型通道与鉴权 | 供应商藏在 `OracleProvider`/`OracleRouter` 后；本地非分发 DEBUG 可受限实验，分发前必须安全后端 | 避免供应商锁定；`.gitignore` 挡不住客户端逆向。另：DeepSeek 官方 API 只有实验性图像**理解**，没有文生图，所以「模型出笔触」在事实层面不成立 |
| 7+8 | Apple 账号与 bundle ID | 现在用 Simulator / 免费账号；bundle ID 是 Xcode 占位值 | 当前不需要 TestFlight；主体、域名与品牌未定，须用户拍板 |
| 10 | 文档结构 | `AGENTS.md`(规则) + `README.md`(说明/计划) + `MEMORY.md`(状态) 三份。`C1-response-quality-set.md`（仓库根）是**数据不是文档**，放模型评测的输入、参考答案与各家实测，性质同 `Tests/` 夹具。2026-09-01 按用户要求去掉了原来的 `Evaluation/` 目录——模型回应都很短，数据量撑不起一个目录，多一层只多一个「东西放哪」的答案 | 三份各有唯一职责。评测集是「拿什么去比模型」的数据，混进文档层会让三份文档的边界失效 |
| 11 | 首个产品目标（用户拍板） | 手写文字进 → 手写文字出。图像理解与图像回应往后排 | 绕开「像素→笔顺」这个已证明很难的问题；文字这条腿的桥是字形笔顺数据，不需要矢量化 |
| 13 | 断网/失败降级 | 只做诚实硬提示，不做假对话 | 半成品假对话比硬提示更伤 |
| 14 | 重播打断 | 用户新落笔即中断并接管，半截字留在页上不回退 | 逐笔重播不能是不可打断的单向表演；半截字最诚实 |
| 15 | 手写识别路线 | iPadOS 27 的 `PKStrokeRecognizer`。**决定不变** | Apple 自家、免费、离线、隐私最好、不花 token，官方支持中文简繁与粤语。模拟器缺中文模型是环境限制，不是平台限制 |
| 17+29+30+31 | 成页触发 | **阈值不是固定秒数**：本页笔间停顿中位数 × 倍数，上下限只作护栏；终止标点按比例加速；等待期可撤销；Pencil 悬停只能**有界延长**、绝不能阻止成页、硬件不支持时不做补偿。量不出节奏时取**上限**（8 秒） | 实测一页字 76% 的时间在停顿、中位停顿 0.51 秒，任何固定秒数对写快和写慢的人必有一方是错的（原「2.8 秒」就是这样误触发的）。漏触发（写完了永远没反应，且没有按钮可救）比早触发严重得多，所以宁可多等。悬停只在 M2+ iPad 配 Pencil Pro/二代 上可用，模拟器和 iPad 10 都没有，因此「没有悬停」不是需要兜底的降级 |
| 18 | 魂的笔画归属 | 用户可以擦掉。动画播完后笔画落定进 `PKDrawing`，与用户笔画同一份数据 | 否则 PencilKit 橡皮擦不到，自己实现橡皮会重复系统能力且手感不一致 |
| 20 | 主界面按钮 | 写字那张纸上零按钮；设置、隐私、删除数据从页边缘划出 | 魔法不能有按钮，但 AGENTS.md 要求的删除数据路径必须可达 |
| 23 | 设备范围 | 仍 iPad-only（`TARGETED_DEVICE_FAMILY = 2`） | 没有任何 iPhone 支持 Apple Pencil，手指在手机上写汉字是另一种交互；现在打开 iPhone 只会让核心功能不可用，属伪装可用。真正的「兼容更多设备」靠 D1 的比例化参数达成，不是翻一个开关 |
| 24 | 书写方向 | 横排竖排都要兼容。输出侧由排版层方向参数决定；**输入侧竖排识别是未知**，已列入 F3 门禁检查项 | 用户要求两者兼容；但识别对中文竖排的支持无法从外部确认，必须实测 |
| 25 | 参数单位（D1） | 只有字高用毫米，其余全是字高的比例；时长用绝对秒 | 真人写小字时手抖幅度也更小，绝对毫米会把小字糊掉；时间不随字号变化 |
| 26+27 | 手写体字体（E8 脚手架，**2026-09-01 已随 H9 整体删除**） | 曾用寒蝉手拙体 v2.500 + CoreText 断行。字体、排版层、渲染层、测试、3.8 MB 字体文件全部删除，已核实 App 包里不再有 `.otf`/`.ttf` | 字体是轮廓不含笔顺，给不出逐笔生长，从一开始就只是脚手架，E1 完成即失去用途。**保留下来的是那条理由**（写进 `GlyphStrokeLayout` 文件头）：CoreText 拿字体量的是**比例字宽**，字形笔顺数据是**等宽字面方格**，拿前者去摆后者的笔画会让字互相压 |
| 28 | 字形笔顺数据授权 | Arphic 1999 版 | 唯一实质区别是商用许可，1999 允许商用；2010 版并非更全，只是更严格 |
| 60 | 画布输入策略（2026-09-01 用户拍板） | **手指、第三方笔、Apple Pencil 全部支持**，即维持 `drawingPolicy = .anyInput`，不做只认 Pencil 的限制 | 用户明确要求「都支持最好」。这也让 `.anyInput` 从「暂定值」变成有依据的决定。代价要知道：手指与第三方笔都没有压感也没有倾斜（决策见风险表），所以引擎自己生成的压感在这些输入下是唯一来源；另外手掌误触的防护由 PencilKit 自己管，我们没有额外处理 |
| 33 | 页面写满怎么办（用户拍板） | **翻页 + 翻页动画**，另加 DEBUG 提示（E3f）。判据是「找不到放得下回应的空位」，已由 E9e 的 `noRoomOnThisPage` 给出 | 静默不回应或把回应压在别人字上都不可接受 |
| 34 | 「这一轮写了什么」怎么切 | 按**落笔时刻**切：成页后记下分界点，之后只有更晚落笔的算新内容 | 不按笔画数量切：擦掉中间一笔再补写一笔数量不变，新内容会被无声漏掉。**纠正过一次错判**：我曾说这取决于版式决定，其实版式管坐标、轮次只管时间，两件事无关 |
| 36+37+38+39+43+44 | A 组手感的形状 | 抖动是**沿弧长的连续摆动**、位移打法线方向，波长由手抖频率与书写速度推出；起收笔用**最小急动度轨迹**；笔间停顿由跳跃距离 ÷ 空中速度推出；墨点直径按 **√进度**；压感与接触**分成两个量**；重采样把总长**等分**。其中 A3/A5/A7/A8 是**零新增参数**的 | 逐点白噪声不是手抖，只是让边缘起锯齿，而且因为难看只能压到看不见、功能形同虚设；法线方向才会把线掰弯。最小急动度曲线的理由是「人就是这么动的」，两端速度与加速度都为零。墨量随时间线性增加而墨点是一片圆、面积∝直径²，所以直径∝√时间。压感揉进接触会让收笔**永远收不到零宽**（压感映射线宽有 60% 下限，那个下限本身是对的）。等分重采样是为了消掉末尾长度接近 0 的碎段——那种碎段让摆动算法线时方向退化，不报错只让收笔偶发不对劲 |
| 40+41+42 | 手写字形怎么做 | 拉丁写**手写印刷体**不做连笔；用 `PenMove` 词汇（墨点/直线/曲线/圆弧）描述而不是裸点列，再把全部字形渲染成字符画人工核对；空格是「有宽度、没有墨迹」的零笔画字形而不是缺字 | 连笔字母的形状取决于前后字母，必须为每种连接关系单独定义（组合爆炸），且连笔英文夹在方块汉字之间反而突兀。裸点列没人看得懂、改错了也看不出来；而「像不像那个字」机器判断不了，唯一能写的「期望形状」就是把实现抄一遍当期望值，等于自己证明自己。空格报成缺字属**假警报**，还会让真正的缺字淹没在噪声里 |
| 45 | 最低系统（用户拍板，取代旧的 27） | 部署目标 **iPadOS 26**，`PKStrokeRecognizer` 用 `#available(iOS 27)` 门控 | 三件待验证的事里只有识别需要 27；连续动画与真实 Pencil 的停顿分布在 26 上就能测。已实测 App 在 iOS 26.5 上跑起来并如实提示 |
| 46 | 「读不出来」要分三种 | 系统太旧 / 有 API 但缺语言模型 / 有模型但认不出，三种分开报 | 解决办法完全不同：升级系统、换设备、写清楚一点。合成一个「不可用」会让用户按错方向 |
| 47+48+54 | 校准（A10）的三条铁律 | 只输出「量出来的 vs 现在用的」，**不自动写回配置**；量不了的项返回「量不了 + 原因」而**绝不给默认值**；报告自证数据来源（用笔与屏幕的夹角是否变化区分笔和鼠标） | 自动写回意味着一次异常采样就能悄悄改掉全 App 的手感且没人知道。给了默认值，「已校准」就成了谎话。而鼠标的速度与抖动不是人手的，拿那批数字调手感等于调到一个不存在的「人」身上，指望人记得「这次是鼠标」不可靠 |
| 49+50 | C1 评测集（用户拍板） | 魂**跟着用户的语言习惯走**，不把中英混写翻译成纯中文；必须含危机场景（第 11 段），且作为**否决题**单独看不并入排名 | 用户写 `review`，回应里出现 `review` 才自然。日记是人在最低谷时会打开的东西，模型在那一段回避或切成客服口吻，无论总分多高都不能用 |
| 51 | 占用图用粗格子（E9b） | 页面切成小方块，碰到墨的方块算占用；格子大小与预留间距都从字高推出 | 要回答的是「这块地方放得下一段话吗」，不是「这两条线相交吗」。精确求交慢且没必要；按笔画包围盒判断又太粗（一条斜撇会吞掉一大片空白）。方向刻意保守：宁可说放不下 |
| 52 | 试排直接复用真排（E9d） | `measure` 就是 `layOut` + 包围盒，不另写只算尺寸的快路径 | 另写就意味着换行规则有两份，迟早不一致，而症状是「试排说放得下、真画出来压到字」，只在特定文字与宽度下出现，极难定位 |
| 53 | 字号换算比例量自字形数据（E9c） | 单笔跨度 ÷ 字高 = 0.49，用打包字形数据当标准答案量出，并由测试重新量一遍断言。**但只对汉字成立，目前只作诊断** | 不换算会把字号估到实际的一半；而写死一个猜的比例，改错了没有任何报错 |
| 55 | 缓存系统识别器（反复一次后定案） | `@available(iOS 27) actor` 按语言列表缓存 `PKStrokeRecognizer`，整个 App 一个 | 曾以「缓存的理由从未测量过」为由改成每次新建。实测证据来了：30 笔刷出约六十条 `Remote connection to handwritingd was invalidated`。用 actor 而不是加锁全局字典，是为了不用 `@unchecked Sendable`（那等于把并发正确性从编译器手里拿回来自己保证） |
| 56 | DEBUG 里可以打识别结果（对隐私规则的刻意破例，用户已确认保留） | 包在 `#if DEBUG` 里，release 构建连代码都不存在 | 不看识别出来的文字就无法判断识别对不对，而缺语言模型时识别器不返回空、会吐出看起来正常的垃圾——没有这一行，那种失败完全无声 |
| 57+58+59 | 回应落点（E9e） | 候选是「你那句话的右边」与「下方」，**顺序按种子随机互换**；都放不下才从页顶往下扫；全放不下报 `noRoomOnThisPage`。间距**必须从占用图推出**（`clearance + cellSize`）而不是取手感值。每个候选位置**自带**可用宽度，没有「收窄重试」这一步 | 固定优先级会让每次回应落在同一个相对位置，一页像表格；硬塞位置的后果是压字。间距那条是踩出来的：第一版凭手感取 0.3 字高，结果右边和下方几乎永远失败——同一条「别贴着写」的规则被写了两遍，占用图撑开时恰好把间距吃回去。而「收窄重试」是我说错的：收窄只会让文字块**变高**，挡路的是下方墨迹时反而更放不下 |

| 61 | 回应必须带邀请（2026-09-01 用户拍板，改了原方向） | **每段回应以一个把话递回来的问句收尾**，回应长度从 10 字上下提到 25–40 字。C1 的 11 段参考答案全部重写 | 原来只要求「克制」，那会得到一个**永远不叫你回来的产品**：每次把你说的话原样收下就结束。而核心循环是「你写 → 它回 → 你再写」，不邀请循环就断在第二步。**边界仍在**：邀请要指向他写过的东西、答起来只要几个字、允许不答；「你为什么会这样想」是问诊，「你有没有试过早点睡」是把建议包装成问句，两者照旧禁止 |
| 62 | Mock 只在 DEBUG（对「绝不返回 Mock 冒充成功」的界定） | `MockOracleProvider` 包在 `#if DEBUG` 里，Release 连代码都没有；且它**完全不读用户写的字**，按轮次轮换固定文字 | 「用 Mock 掩盖一次失败」和「真 provider 还不存在所以先用假的开发」是两件事。前者是伪装成功，后者是正常开发顺序。刻意不做「按关键词挑答案」的假聪明——一旦看起来像懂了，就没人能分清「模型不行」和「Mock 在骗人」。轮换是明显机械的，不可能被误认为理解 |
| 63 | 识别器抽成协议（E6a 顺带） | `HandwritingReading` 协议，`DiaryPageModel` 注入 | 真识别器读不出测试里造的合成笔画（几条横线不是字），于是「问魂」那一环在测试里**永远走不到**，只会停在「这一页没读出字来」。识别依赖设备上装了什么语言模型，那是环境不是我们能在测试里控制的东西——和 Oracle、笔画源属同一类，都该可替换 |
| 64 | 装配顺序不能反（E6a） | 必须「定落点 → 按落点给的宽度排版 → 手绘化」。`ReplyComposer` 是唯一的装配点 | 先排版再找位置会得到一个宽度不合的文字块：排版时假定的行宽和落点实际能给的宽度不一样，画出来就压字或留一大片空白。而装配只能有一处——原先预览里有一份、正式路径要另一份，那必然出现「预览里好看、App 里错位」 |

已排除（实现过或讨论过，已删且不留死代码）：位图抽骨架路线、Python 位图/向量对比 spike、预制笔画母题库（组合太少）、图像生成与图像回应、Magic Stroke Lab 与离线夹具、渲染层的自动缩放与卡片外观。

## 风险与缺口

| 风险 | 处置 / 状态 |
|---|---|
| 魔法体验不动人（生死项） | E7 手写文字回应的 Go/Kill 评审仍待做。E1 + E9e 已完成，逐笔生长在 Xcode 预览里能看，**已具备初判条件**；但连续动画的真实观感仍受模拟器停帧限制（见下一行），正式评审要真机 |
| **`HandwritingReplayView` 的连续动画在模拟器里能不能动，现在可以测了但还没测** | 已知事实：2026-08-27 在 E8 的另一个视图上实测，`TimelineView` 两种调度在模拟器里都只出一两帧就停；`@State` 触发的重绘照常。本视图此前无法触发，E6a 之后 DEBUG 构建里写几个字就会触发。**我自己验不了**：模拟器上画笔画需要自动化鼠标拖拽，而本机 osascript 没有辅助访问权限，`simctl` 也没有画笔的接口——只能由用户实跑。修法已备好：把驱动重绘的东西换成推进 `@State` 的异步循环（落墨进度是从**单调时钟**算的、不是累加 tick，丢帧不会画错，所以换驱动安全），但刷新率**不能写死 60**（原实现写死 1/60 会让 120 Hz ProMotion 只跑一半） |
| **E9c 的字号估算不能用来决定魂的字号** | 实测偏 2–2.5 倍。根因：0.49 那个换算比例只对汉字成立——人写拉丁字母时一笔常跨过整个字高（比例接近 1），涂鸦也被当成很高的字。现在报告同时给原始跨度并标注只作诊断。已想到的修法是用「每字几笔」区分文字（汉字 5–十几笔/字、拉丁 1–2 笔/字），但它依赖识别可用，而模拟器上中文识别本身就是坏的 |
| **手感数值全部没有真人依据**（贯穿 A 组、A10、E3c） | A 组**结构**已修完，其中 A3/A5/A7/A8 零参数。但 A1/A2/A4 七个数值、成页阈值的倍数与上下限（倍数 6、下限 1.5s、上限 8s、标点比例 0.45、悬停宽限 2.5s）、行距 1.6、页边距 8% 短边、用户笔宽 0.5 mm，全部只是量级推算或一次模拟器鼠标书写作参照。量尺（`HandwritingCalibration`）已就位；**用户已决定暂缓真笔校准**，先用模拟器手迹把框架跑通。在那之前不得称手感已验证 |
| 识别准确率的已知边界 | 拉丁字母**可用且质量不低**（模拟器鼠标写 `hi hello I loveyou!?` 基本全对；首字母大写是识别器的句首规整不是错误）。中文在**模拟器上恒为坏的**：iOS 26.5 与 **27.0 模拟器都缺中文模型**（根因是 `CoreHandwriting.framework` 里中文只有码表没有本地权重，非拉丁模型靠系统资产按需下载而模拟器不带）。而缺模型时识别器**返回垃圾而不是 nil**（「你好」→`15.47`，含中文的页→`#31`），且无从事后过滤——只能靠 E4b 在落笔前就告知。此前「识别率不高」的观感全部来自中文那段。备选路线：Vision `RecognizeTextRequest` `.accurate` 支持 `zh`/`zh-TW`/`yue`，代价是丢掉笔画顺序且面向印刷体 |
| 已解决，留一行避免误以为遗漏 | ~~成页时识别整页~~ ✅ 按落笔时刻切轮次（E3e）。~~中文手写识别是头号风险~~ ✅ 用户在 iPad 10 + iPadOS 26.6 + USB-C Apple Pencil（无压感）上用系统 Scribble 手写中英混写全部认对——**中文模型就在真机上、质量够用、不需要压感**。仍未验证的只剩 `PKStrokeRecognizer`（27 的 API）能不能用上同一套模型；两者几乎确定共用 `CoreHandwriting`，风险已降为一次接口确认 |
| Scribble 不能当输入通道（已判断，不是待办） | 它会把笔迹**吃掉**变成文字，而本产品的核心是手写必须**留在纸上**。只能当能力探针 |
| 每抬一次笔就识别一次整页（已知代价，非 bug） | 终止标点加速信号必须在成页**之前**就知道你写了句号，而识别是异步的，等到成页再识别永远来不及。代价是识别频率随页面变长而变贵。真机测出延迟后若太贵，就降级成只在成页时识别、同时放弃标点加速（属计划 B） |
| **压感数据在当前硬件上永远拿不到**（已实测确认） | 模拟器鼠标写字 `force` 精确为 0.000。USB-C 款 Apple Pencil 无压感传感器；第三方笔经 PencilKit 也拿不到压感（个别宣称有的只在集成其厂商 SDK 的 App 内有效）。**对当前硬件「没有压感」是常态而非例外。** 引擎自己生成压感所以照常工作，但 A10 的力度维度与 C2 的力度信号除非补一支 Apple Pencil 一代（用户已决定不补）否则永远无数据。速度、停顿、涂改次数、字大小不受影响 |
| 用户的字与魂的字质感不统一（实测发现） | 用户笔画细而均匀（固定宽、无压感），魂的字有明显粗细变化。同一页上不像同一支笔写的。原因一半是输入无压感，一半是 `userInkWidthInMillimeters` 可能偏细。待有压感的笔后重新评估 |
| **App 没有本地化文件** | 界面文案全是硬编码中文，没有 String Catalog。已把工程 `developmentRegion` 从 `en` 改成 `zh-Hans` 让现状自洽（否则语言名会显示成 "Chinese" 而界面是中文），**但这不等于做了本地化**；真要出海所有硬编码文案都要搬 |
| **中文断行规则仍然没有** | 句号可能出现在行首、「」可能被拆到两行、行尾空格没折叠。CoreText 会做这件事，但它的比例字宽模型与等宽字面方格不兼容，所以 `GlyphStrokeLayout` 里没有 |
| 手写定义的字形没有上游可比对（E1c/E1d） | 27 个标点 + 62 个拉丁字母数字的形状由我按印刷体常见样子写出。可机器验证的部分有测试守着，但「像不像那个字」只能靠眼看——已渲染成字符画人工核对。最没把握的是**顿号 `、` 的倾斜方向**，已在代码里单独标注 |
| 永久 Key 泄露 | 见 `AGENTS.md` 红线；本地 DEBUG 例外须最小权限 + 额度 + 监控 |
| 无证据承诺「不出境 / 读完即删」 | 设计目标与供应商事实分开，逐项验证 |
| 内部 IP 占位进入公开面 | 门禁 + staged diff 人工审查；分发前原创化。注意 `AGENTS.md` 的「IP 与合规」节已被用户删除，发布前清单（年龄分级 / AI 披露 / 自残危机兜底 / 删除全部数据 / 跨境复核）目前无文档承载，用户已明确不恢复 |
| 字形笔顺数据许可 | makemeahanzi / hanzi-writer-data 派生自 Arphic 1999 公共授权字体，分发前须复核授权义务；本地非分发实验不受阻塞 |

已接受但受限：项目名、仓库名、占位 bundle ID、本地 persona 只限私有非分发面，不代表商业授权。

## 模型与 Key 边界（2026-07-23 核对官方说明）

- Key 按区域区分；部分区域（北京、新加坡、东京、法兰克福等）调用需把 Workspace ID 放进 Base URL，接入时以实际账号页面为准。
- 移动端正式调用经安全后端；官方建议由后端为不可信环境签发临时 Key（默认 60 秒，可配 1–1800 秒，继承签发 Key 权限）。参考：[Qwen 首次 API 调用](https://help.aliyun.com/en/model-studio/first-api-call-to-qwen)、[临时 API Key](https://help.aliyun.com/en/model-studio/application-obtain-temporary-authentication-token)。
- 未核实：模型实时可用性/配额/价格、大陆到端点的可达性与延迟、供应商对请求/日志/备份/训练/删除的条款、「数据不出境」的合同与全链路证据、自部署版本许可证与运维责任。

## 待用户决定 / 待用户执行

1. **E6（Oracle + Mock 闭环）是下一个大步。** 落点已不挡路。用户已定：真机要等一段时间，**先在模拟器上跑通**；中文识别在模拟器里恒坏，所以模拟器演示走英文输入，输出侧写中文没问题（那是我们自己的字形数据，与系统识别无关）。
2. **C1 参考答案等用户审。** 11 段每段我都写了一个「日记之魂应该怎么回」的参考答案（`C1-response-quality-set.md`）。它是后面比模型的基准，也是写 system prompt 的依据，所以必须先对齐；对齐后用户接各家 API key，我再填「各家实测」。有两处我自己标了拿不准：第 4 段「自己变响」这个比喻可能偏文学；第 9 段刻意选了保守写法（避开量词而不是悄悄用对）。
3. **E6b（危机场景怎么写出来）等用户拍板。** 第 11 段的回应约 70 字，按 1.5 字/秒要写将近一分钟——让处于那种状态的人等一分钟才看到求助电话是坏设计。三个方向：E6b-1 这段不逐笔写直接整段浮现（破坏手写统一感）、E6b-2 先写号码那两行再写其余（救命信息优先）、E6b-3 这段用更快的书写速度（更急，本身也是表达）。我倾向 E6b-2。
4. 正式 bundle ID、签名主体、原创公开品牌与 Persona 方向；何时购买 Apple Developer Program。
6. 真机到手后要验三件（模拟器都验不了）：`PKStrokeRecognizer` 中文是否可用、连续动画是否正常、真实书写的停顿分布。

## 用户偏好

- 中文交流，大白话解释术语，需要类比与详细但易懂的说明；计划编号统一 `A1`/`B2` 格式并在括号里说明这步是什么。
- 区分「已验证事实 / 合理假设 / 待用户决定」，不混写。不静默兜底：阻塞、测试失败、远端分叉必须暴露。
- 不替用户拍板命名、bundle ID、品牌、付费与架构方向。用户会明确要求「先不要进行下一步」，必须停住等确认。
- 主动提醒废掉的逻辑链、失效参数、乱写的硬编码值。
- 重视真实可演示，不接受把命令 exit 0 当成体验通过。每次实质任务收口自动执行安全 Git 同步。

## Agent 自身的判断错误（记下来避免重犯）

- **把「模拟器返回了什么」当成「平台支持什么」**（2026-08-27）：只依据模拟器上 `supportedLanguages` 返回 20 种拉丁语言，就断言 PencilKit 不支持中文，还据此回滚了最低系统。教训：运行时能力查询反映的是**当前环境装了什么**，不是平台契约；两者不一致时先找原因。同一错误的第二个表现是把结论放大成「输入端整体换路」，而正确结论是「拉丁照用，只有中文这一格待验证」——能力缺失要定位到最小范围。
- **同一条规则在两处独立取值**（2026-08-31，E9e）：「回应与用户笔迹隔多远」我在落点决策里凭手感给了个参数，而占用图里已经有一个 `clearance` 表达同一件事，两者恰好互相抵消，症状是两个主候选几乎永远失败。凡是发现自己在为一条已有规则再取一个数，先去找那条规则原来的值。
- **拿陈旧的构建产物当验证依据**（2026-09-01，H9 收口时）：我声称「已核实 App 包里不再有字体文件」，
  用的是 `find ~/.../DerivedData/TomRiddlesDiary-*/... | head -1`——而那个 glob 匹配到两个目录，
  `head -1` 挑中的是 **8 月 26 日**的旧构建，而字体是 8 月 27 日才加进来的。
  也就是说那次「核实」检查的是一个从来没有过字体的包，**什么都没证明**。
  下次要验构建产物：用 `-derivedDataPath` 指定一个刚删空的目录、当场构建、再检查它，
  并且核对产物的时间戳。凡是「我核实过了」，都要能说出核实的对象是哪一次构建。
- **以「好处从未测量」为由拆掉现成的缓存**（2026-08-31，识别器）：理由听起来严谨，但拆掉之前没有测量代价。结果实测每抬一次笔都在拆建跨进程连接。要动现成的性能措施，先量代价。

## 已知陷阱

- **模拟器空闲时冻结渲染循环**（实测）：`TimelineView` 两种调度都只出一两帧就停；`@State` 触发的重绘照常。不要因为模拟器里不动就以为代码错了——这和中文识别模型缺失是同一类环境限制。
- **`@Observable` 不比较新旧值**（2026-08-29，代价是一个真 bug）：把同一个值再写一遍，观察者照样收到通知、界面照样重建。倒计时每 0.1 秒写一次 `phase`（大多数次值没变），界面每秒重建十次，PencilKit 墨迹层被反复标脏却一直没重绘——**用户写的字从纸上消失了**，而数据全在、纸色也在。凡是高频赋值给 `@Observable` 属性，都要先比较。
- **两个配置常量之间的隐藏耦合**（被测试撞出来的，不是事先想到的）：成页倒计时每 0.1 秒轮询，而最短预告期是 `shortestWait × hintFraction`；后者一旦短于前者，一次轮询就跨过整个预告期，「可撤销的预告」直接消失，症状只是「提示时有时无」。已加断言守住。
- **浮点累加顺序会让「刚好画完」差一个尾数**：凡是「累加出来的总量」和「逐步推进的游标」要对得上的地方，两边必须用同一种累加方式，否则最后一笔差一点点画不完，没有任何报错。
- Swift 6 下**不能**给 `XCTestCase` 子类整体标 `@MainActor`（与继承来的 nonisolated 初始化方法冲突，直接编译报错）。要测 `@MainActor` 类型时，隔离标在具体测试方法上。
- 两条来自系统的日志噪声，不是 bug：`Invalid reflowable tokens count` / `Skipping adding principal lines to token` 来自系统的 `handwritingd`；`UIKitHoverGestureRecognizer ... has been in possible phase for N seconds` 是因为当前所有可用硬件都不支持悬停，识别器永远停在 possible 阶段。
- Xcode 26.6 并行 test clone 触发 `DVTiPhoneSimulator` assertion → 测试关闭并行。工程用 objectVersion 77 同步文件组，`Sources/`、`Tests/` 下新增文件自动入 target。kiro-pet 已迁至 `110_KiroPet`，不得放回本仓库。

## 历史（精简）

<!-- 只留「以后还会影响判断」的结论。过程细节由 Git 历史承载。 -->

- **2026-07-15 至 08-27（起步、地基、输出侧脚手架）**：仓库骨架、规则/记忆/脚本、GitHub 私有远端、Xcode 工程纳入根仓库、修正为仅 iPad。建立单位契约与集中配置（D1–D6）。删掉位图路线与 Magic Stroke Lab，文档收敛为三份。用户拍板首个产品目标「手写文字进 → 手写文字出」。打包寒蝉手拙体 + CoreText 断行（E2/E8），第一次看到回应出现在纸上。留下三条还在生效的教训：把模拟器读数当平台契约（见上节）；排版失败返回 nil 让纸上空白无提示（正是自己要防的静默兜底，改成显式三态）；模拟器空闲冻结渲染循环。
- **2026-08-28（输入端接通 + 字形笔画 + 成页与打断）**：升 Swift 6 并给纯逻辑层加 import 白名单门禁（当场应验：编译器直接挑出 `PencilStrokeReader` 不该绑主线程）。接上 PencilKit 画布，引擎第一次有真实输入源；压感刻意不伪造。E1：`graphics.txt` 30.78 MB 只留 medians 压到 5.26 MB（9574 字），刻意**不复用 `TextLayout`**（字体是比例字宽、字形数据是等宽方格，混用会让字互相压）。E3c 把成页阈值从固定秒数改成停顿中位数 × 倍数。E3d 把渲染层入参改成 `ReplayPlayback` 三态（原契约表达不了「停在当时的进度」，只能被迫映射成「画完」，正好把「你打断了它」抹掉）。同轮撞出：缺语言模型时识别器返回垃圾而不是 nil。
- **2026-08-29（修自己造的 bug + E3e + E4b + A 组 + 字形补齐 + C1）**：先修「用户写的笔画写完就从纸上消失」（根因见已知陷阱里的 `@Observable`）。E3e 按落笔时刻切轮次；E4b 落笔前就查识别能力并如实告知，过程中发现工程 `developmentRegion` 是 `en` 导致语言名显示成 "Chinese"，改成 `zh-Hans`，并记下「没有本地化文件」这个真实缺口。A1–A5、A7、A8 全部完成，其中四项零参数。E1c/E1d 手写 27 个标点 + 62 个拉丁字母数字，用 `PenMove` 词汇 + 字符画人工核对。被测试撞出三个真问题：浮点累加顺序、空格被报成缺字、墨点与线段对 infinity 处理不一致。用户实测 Scribble 排除了中文识别这个头号风险。
- **2026-08-30（降到 iPadOS 26 + 清脚手架 + A10 量尺 + E9a–E9d）**：部署目标降到 26，识别用 `#available(iOS 27)` 门控。发现测试套件一个真缺口：6 条识别用例假设识别器一定存在，在 26 上全红——改成 `XCTSkip` 显式跳过而不是塞空分支（空分支显示「通过」，那是假绿）。清掉全部开发期脚手架，纸上只留 E4b 那句真提示。新建 `Calibration/`（三条铁律见决策 47）。E9a–E9d 建起落点决策的全部输入，其中最有价值的一步是字号换算比例：原本单笔跨度直接当字高用，实测系统性偏小一半，于是拿打包字形数据量出 0.49 并加测试重新量一遍断言——把一个会静默出错的猜测换成被机器盯着的测量值。
- **2026-08-31（模拟器实测三件 + E9e 收口）**：实测确认墨迹不再消失、成页与识别链路跑通、A10 的「数据来源」自证正确判成鼠标（`penLiftDuration` 拟合出负数时也如实报「量不了」）。当轮处理三件：恢复识别器缓存（见决策 55 与判断错误第三条）；字号估算偏 2–2.5 倍改为只作诊断；加一行 DEBUG 专用识别输出。**并确认 iPadOS 27 模拟器仍然缺中文模型**（`可用[en] 缺失[zh]`，英文认得准）。然后完成 E9e（见决策 57 与判断错误第二条），并把 `SeededRandomNumberGenerator` 从 `StrokeHumanizer` 提到 StrokeEngine 共用（复制第二份会让两处实现分叉，症状是所有手感回归测试同时失去意义）。284 测试在两个系统上通过。
- **2026-09-01（H9 + C1 改向 + E6a 链路接通）**：删掉 E8 字体脚手架（5 文件 + 3.8 MB 字体），理由保留在 `GlyphStrokeLayout` 文件头。C1 按用户意见改向两次：先是「参考答案要我先写、不能留待填」，再是「答案太短且必须带问句鼓励继续写」——第二条推翻了我原来只求克制的设计，因为只有克制会得到一个永远不叫你回来的产品；11 段全部重写，评分表加「邀请」一项（总分 132→165）。然后完成 E6a：`OracleProvider` 协议 + DEBUG 专用 Mock + `ReplyComposer` 装配点，接进 `DiaryPageModel`，**那条断了很久的链路终于通了**。过程中三件值得记：一、识别器不抽成协议的话「问魂」在测试里永远走不到（真识别器读不出合成笔画）；二、`isCommitted` 一开始把 `.understanding` 算成 true，导致「只读新写的笔画」那条断言拿到整页——成页进行中不等于成页完成；三、**我上一轮说「已核实 App 包里没有字体」是错的**，验证时挑到了 8 月 26 日的旧 DerivedData（详见判断错误一节）。用新构建重验：包里只剩 5 MB 字形数据，二进制里连 `ChillZhuo` 这个名字都搜不到。282 测试在 iOS 26.5 与 27.0 上都通过。
