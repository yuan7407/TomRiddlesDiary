<!-- 动态状态与决策。会话开始或用户说“继续”时先读本文件。规则见 AGENTS.md，项目说明与计划见 README.md。保持 ≤120 行。 -->
# 项目记忆

## 当前状态

- **阶段**：输入端整条链路已通（E3a→E3b→E3c→E3d→E4 接线），输出端逐笔生长已通（E1a/E1b + E2 + E8），**回应落点 E9a–E9e 全部完成**。计划编号见 `README.md` 的 A–H 表；字母是名字，执行先后见那里的「执行顺序」。
- **链路上唯一缺的一环是 Oracle（E6）**：用户写字 → 停笔 → 成页 → 识别出文字 → **断在这里**。文字拿到了，没有东西能把它变成回应。接上之后 `DiaryPageModel.beginReply` 就是入口，后面的定落点（E9e）、排笔画、手绘化、逐笔重播、落笔中断全部已就绪并可运行；`DiaryPageView` 的 Xcode 预览里这条链路（含落点）已经能真正跑起来，用的是写死的假文字，所以刻意不进 App。
- **App 跑起来的真实样子**：一张能写字的纸；停笔后左上角渗墨（预告，再写一笔即取消）；不写就成页并识别；成页后页面**如实说明魂还没接入**，不假装在思考。DEBUG 读数在左下角，显示笔数、停顿、算出来的成页阈值、当前阶段、识别结果。
- **引擎**：`StrokePipeline → StrokeHumanizer → StrokeReplayTimeline`，入口只吃 `[Polyline]`（有笔顺的有序坐标）。两个真实生产者都已就位：`PencilStrokeReader`（用户手写）与 `GlyphStrokeLayout`（字形笔顺）。
- **渲染层**：`Features/Response/HandwritingReplayView`，入参是 `ReplayPlayback` 三态（在播 / 停在某进度 / 已写完）。坐标 1:1，不缩放。
- **单位契约（D1/D2 已完成）**：坐标一律页面点；**只有字高用毫米声明**（默认 9 mm ≈ 46.8 pt），间距、抖动、速度、墨宽全部是字高的比例。`Configuration/` 下四个文件是经验性常量的唯一定义处：`PageMetrics`（唯一毫米↔点换算）、`HandwritingFeel`（手感 + 装配入口）、`PageAppearance`（纸墨与说明文字浓度）、`InteractionSettings`（怎么算一次输入 + 成页参数）。引擎已无默认参数。
- **验证**：284 个 XCTest 在 iOS 26.5 与 27.0 两个模拟器上都通过（26 上 4 条识别用例如实跳过）；Simulator build 成功；空白页截图人工确认。测试必须加 `-parallel-testing-enabled NO`。
- **未接入**：Oracle、任何网络或密钥、加密存储、后端、TestFlight。
- **目录**：`Sources/` 下为 `Configuration / Features(Canvas·Response) / Handwriting / StrokeEngine / Resources` + 入口。

## 决策表

| # | 决策 | 结论 | 为什么 |
|---|---|---|---|
| 1 | 核心交互 | 手写文字，不做文字聊天复制品；涂鸦识别往后排 | 笔画与逐笔节奏是差异化内核 |
| 2 | 手感来源 | 本地引擎负责，不靠模型输出最终笔画 | 模型笔画不稳定，节奏与压感必须可控 |
| 3 | 笔画源 | 引擎只吃有序向量 `[Polyline]` | 位图抽骨架实机效果差且已删；手打夹具也已删。代价是当前零个生产者，两个真实来源（字形笔画中线、PencilKit 手写）都待建 |
| 4 | 模型通道 | 视觉/文本模型藏在 `OracleProvider`/`OracleRouter` 后，具体供应商不写进业务代码 | 避免供应商锁定；DeepSeek 官方 API 只有实验性图像**理解**（`deepseek-v4-flash-vision-exp`），没有文生图，所以「模型出笔触」在事实层面不成立 |
| 5 | 模型鉴权 | 本地非分发 DEBUG 可受限实验；分发前必须安全后端 | `.gitignore` 挡不住客户端逆向 |
| 6 | Persona | 内部占位可替换、环境门控；公开版原创 | 体验探索与 IP 风险隔离 |
| 7 | Apple 账号 | 现在用 Simulator/免费账号 | 当前不需要 TestFlight |
| 8 | bundle ID | Xcode 占位值，非正式决定 | 主体、域名与品牌未定，须用户拍板 |
| 9 | 规则源 | 根 `AGENTS.md` 唯一 | 避免多份规则漂移；已删除 `CLAUDE.md` 指针文件 |
| 10 | 文档层级 | `AGENTS.md`(规则) + `README.md`(说明/计划) + `MEMORY.md`(状态) | 三份各有唯一职责，不再有 topics 分散 |
| 11 | 首个产品目标（2026-08-26 用户拍板） | 手写文字进 → 手写文字出：用户用 Pencil 手写文字，一段时间后收到逐笔手写的文字回复。图像理解与图像回应往后排 | 绕开「像素→笔顺」这个已证明很难的问题；文字这条腿的桥是字形笔顺数据，不需要矢量化 |
| 12 | 预制笔画母题库 | 不考虑 | 组合太少、方法太笨（用户判断） |
| 13 | 断网/失败降级 | 现阶段只做诚实硬提示，不做假对话；世界观内的断网动画留待以后 | 魔法要完整才有意义，半成品假对话比硬提示更伤 |
| 14 | 重播打断 | 用户新落笔即中断并接管，半截字留在页上不回退 | 逐笔重播不能是不可打断的单向表演；半截字最诚实 |
| 15 | 手写识别路线 | 走 iPadOS 27 的 PencilKit 内建识别（`PKStrokeRecognizer`）。**决定不变** | Apple 自家、免费、离线、隐私最好、不花 token。官方支持中文简繁与粤语。模拟器缺中文模型只是环境限制，不是平台限制 |
| 16 | 图像回应 | 砍掉，不是暂缓。相关素材规格、占位 key、管道描述全部删除 | 已废逻辑链留着会持续误导 |
| 17 | 成页触发 | 长停顿为主 + 终止标点加速 + 等待期可撤销视觉预告 + Pencil 悬停（有则用之）。阈值必须由真实书写数据量出 | 无按钮；漏触发比误触发严重；猜错必须零代价可救 |
| 18 | 魂的笔画归属 | 用户可以擦掉。动画播完后笔画落定进 `PKDrawing`，与用户笔画同一份数据 | 否则 PencilKit 橡皮擦不到，自己实现橡皮会重复系统能力且手感不一致 |
| 19 | 渲染层去留 | 不整体删，删 60% 保 40%（搬家瘦身） | 分段绘制与半段插值是逐笔生长的实现核心，且不会因 B1 作废；自动缩放与卡片外观才是错的 |
| 20 | 主界面按钮 | 写字那张纸上零按钮；设置、隐私、删除数据从页边缘划出 | 魔法不能有按钮，但 AGENTS.md 要求的删除数据路径必须可达 |
| 21 | 输出侧分两阶段（2026-08-27 用户拍板） | 先用手写体字体跑通链路（E8），再引入字形笔画数据实现真正逐笔生长（E1）。字形笔画是确定要做的，不是备选 | 字体版本能立刻验证排版与时序且不依赖任何升级；但字体是轮廓不含笔顺，给不出逐笔生长，所以只是脚手架，不能用它判生死（E7 必须等 E1） |
| 22 | 最低系统 | iPadOS 27.0（已落地）。构建必须用 Xcode 27（命令行用 `DEVELOPER_DIR` 指定，不改全局 `xcode-select`） | PencilKit 内建识别只有 27 有。代价：beta 期间无法上 TestFlight/App Store，正式版约九月 |
| 23 | 设备范围 | 现阶段仍 iPad-only（`TARGETED_DEVICE_FAMILY = 2`）。iPhone / 折叠机等真机存在且有对应设计后再谈 | 折叠 iPhone 未发布，规格未知；且没有任何 iPhone 支持 Apple Pencil，手指在手机上写汉字是另一种交互。现在打开 iPhone 安装只会让核心功能不可用，属伪装可用。真正的「兼容更多设备」靠 D1 的比例化参数与零硬编码尺寸达成，不是靠翻一个开关 |
| 24 | 书写方向 | 横排竖排都要兼容。输出侧由排版层的方向参数决定，代价小；**输入侧竖排识别是未知**，已加入 F3 门禁的检查项 | 用户要求两者兼容；但识别对中文竖排的支持无法从外部确认，必须实测 |
| 25 | 参数单位（D1 结论） | 只有字高用毫米，其余全是字高的比例；时长用绝对秒 | 真人写小字时手抖幅度也更小，绝对毫米会把小字糊掉；时间不随字号变化 |
| 26 | 手写体字体（2026-08-27 用户拍板） | 寒蝉手拙体 v2.500，中英文都用它 | 作者 README 明确「免费授权全社会使用（包括商用）」，请求标注作者为寒蝉字型。实测拉丁与汉字都是不等宽比例字宽（A=490/a=314/我=858/的=631，每 em 1000），比等宽更像手写。GB2312-80 字符集、7586 字形；**生僻字会缺字**，届时会显示豆腐块 |
| 27 | 排版实现 | 用 CoreText 断行定位，不自己按格子排 | 这个字体连汉字都不等宽，按格子排会挤或散；中文换行规则（标点不能在行首等）自己写必然漏 |
| 28 | 字形笔顺数据授权 | Arphic 1999 版 | 唯一实质区别是商用许可，1999 允许商用；2010 版并非更全更强，只是更严格。个人非商用用 1999 已足够 |
| 29 | 成页阈值怎么定（E3c 结论） | **不是固定秒数**：阈值 = 本页笔间停顿的中位数 × 倍数，上下限只作护栏 | 实测一页字里 76% 的时间在停顿、中位停顿 0.51 秒，任何固定秒数对写快的人和写慢的人必有一方是错的（原「2.8 秒」就是这样误触发的）。倍数随用户自己的节奏缩放，是唯一有物理含义的参数 |
| 30 | 量不出节奏时等多久 | 取**上限**（8 秒），不取下限 | 只写了一笔时没有笔间停顿可测。漏触发（写完了永远没反应，且没有按钮可救）比早触发严重得多，所以宁可多等——但因为有上限，它终究会响 |
| 31 | Pencil 悬停的地位 | 只能**有界延长**等待，绝不能阻止成页；硬件不支持时不做任何补偿 | 悬停只在 M2 及更新的 iPad 配 Apple Pencil Pro/二代 上可用（[官方规格](https://support.apple.com/en-us/111889)），模拟器和 iPad 10 都没有。它是加分信号，主信号不依赖它，因此「没有悬停」不是需要兜底的降级 |
| 33 | 页面写满怎么办（2026-08-29 用户拍板） | **翻页 + 翻页动画**，另加 DEBUG 提示。属计划 E3f | 静默不回应或把回应压在别人字上都不可接受。依赖回应落点先实现——「写满」的判据是「找不到放得下回应的空位」 |
| 34 | 「这一轮写了什么」怎么切（E3e 结论） | 按**落笔时刻**切：成页后记下分界点，之后只有更晚落笔的算新内容 | 不按笔画数量切：擦掉中间一笔再补写一笔数量不变，新内容会被无声漏掉。**纠正一次此前的错误判断**：我曾说这取决于版式决定，其实两件事无关——版式管坐标，轮次只管时间 |
| 35 | 模型评测集是文字还是手写图（C1 澄清） | **文字**。8–10 段你会写进日记的话（直接打字）+ 期望回应 | Oracle 只收得到文字，识别是另一条链路。混在一起会让「哪个模型更懂情绪」的结论被识别错误污染。手写样张属识别准确率测试（E4），是另一件事 |
| 37 | 起收笔加减速用什么曲线（A3 结论） | **最小急动度轨迹**（Flash & Hogan 1985 的人类点到点运动模型），零参数 | 随便挑一条 ease-in-out 也能让线不匀速，但那要回答「为什么是这条」。这条曲线的答案是「因为人就是这么动的」，而且它两端的速度与加速度都为零，正好是「从静止起笔、停在静止」 |
| 38 | 笔间停顿怎么定（A4 结论） | 由**抬笔跳跃距离 ÷ 空中速度 + 固定抬落笔耗时**推出，不拍固定秒数 | 同一个字里挨着的两笔间隔短，跨到下一个字的跳跃间隔长。固定秒数会让紧挨着的两笔出现莫名停顿，也让跨字跳跃显得太急 |
| 39 | 墨点怎么长（A7 结论） | 直径按 `√进度`，零参数 | 墨量随时间线性增加，而墨点是一片圆、面积∝直径²，所以直径∝√时间。线性增长看起来是气球在匀速放大，不是墨在洇开 |
| 40 | 拉丁字母写成什么体（E1d 结论） | **手写印刷体**，不做连笔草书 | 连笔字母的形状取决于前后字母，一个字母不是一个固定形状，必须为每种连接关系单独定义（组合爆炸）；而且连笔英文夹在方块汉字之间反而突兀 |
| 41 | 手写字形怎么保证质量 | 用 `PenMove` 词汇（墨点/直线/曲线/圆弧）描述而不是裸点列，再把全部字形渲染成字符画人工核对 | 裸点列没人看得懂、改错了也看不出来。而「像不像那个字」机器判断不了，唯一能写的「期望形状」就是把实现抄一遍当期望值，等于用自己证明自己 |
| 43 | 压感与接触分开（A5 结论） | `StrokeSample` 拆成 `pressure`（按得多重，映射到线宽 60%…100%）与 `contact`（笔尖有多少落在纸上，起收笔为 0，直接乘在线宽上） | 之前揉成一个数，起收笔靠压低压感表现渐细；但压感映射到线宽有 60% 下限（那个下限本身是对的：手写时线不会细成头发），于是渐细最多收到 60%，**永远收不到零** |
| 51 | 占用图用粗格子（E9b 结论） | 把页面切成小方块，碰到墨的方块算占用；格子大小与预留间距都从字高推出 | 要回答的是「这块地方放得下一段话吗」，不是「这两条线相交吗」。精确求交慢且没必要；按笔画包围盒判断又太粗（一条斜撇的包围盒会吞掉一大片空白）。方向刻意保守：宁可说放不下，也不说放得下却压字 |
| 57 | 回应落点的候选与顺序（E9e 结论） | 候选是「你那句话的右边」与「下方」，**顺序按种子随机互换**；都放不下才从页顶往下扫；全放不下报 `noRoomOnThisPage` 而不是硬塞位置 | 用户要的是「随机空位」。固定优先级会让每次回应都落在同一个相对位置上，一页写下来像表格。而硬塞位置的后果就是回应压在用户的字上——报错让调用方必须处理（翻页），这正是不静默兜底 |
| 58 | 「隔多远」必须从占用图推出，不是审美参数（E9e 踩坑后定案） | `gap = clearance + cellSize`，扫描步长 = `cellSize`。落点决策里只剩两个真正要人定的数（最小行宽 6 字、抖动 0.25 字高） | 第一版我给了个 `gapInGlyphs = 0.3` 凭手感取值，结果**右边和下方两个候选几乎永远失败**，回应总掉进扫描退路。根因是同一条「别贴着写」的规则被写了两遍：占用图查询时会把候选区域按 `clearance` 撑开，恰好把 gap 吃回去，撑开后的边缘正好落在用户笔迹右端所在的那个格子里，判为占用。凡是两处独立取值表达同一条规则，迟早互相打架 |
| 59 | 试排宽度不是独立旋钮（E9e 更正我此前的说法） | 每个候选位置**自带**可用宽度（从它到页面右边缘），按那个宽度试排；没有「收窄重试」这一步 | 我此前描述成「假定宽度→放不下就收窄重试」，那是错的：**收窄只会让文字块变高**，如果挡路的是下方的墨迹，收窄之后更放不下 |
| 52 | 试排直接复用真排（E9d 结论） | `measure` 就是 `layOut` + 包围盒，不另写只算尺寸的快路径 | 另写一套就意味着「怎么换行、每个字多宽」有两份规则，迟早不一致；而不一致的症状是「试排说放得下，真画出来压到字」——只在特定文字与宽度下出现，极难定位。宁可多算一遍 |
| 53 | 字号换算比例量自字形数据（E9c 结论） | 单笔跨度 ÷ 字高 = 0.49，用打包的字形数据当标准答案量出来的，并由测试从真实数据重新量一遍断言 | 单笔的纵向跨度只有整个字高的一半左右。不换算会把字号估到实际的一半，魂就会写出一页明显偏小的字。而写死一个猜的比例，改错了没有任何报错 |
| 55 | 缓存系统识别器（2026-08-31 反复一次后定案） | 用 `@available(iOS 27) actor` 按语言列表缓存 `PKStrokeRecognizer`，整个 App 生命周期一个 | 曾以「缓存的理由从未测量过」为由改成每次新建。实测证据来了：30 笔刷出约六十条 `Remote connection to handwritingd was invalidated`——每抬一次笔都在拆建一条到系统手写守护进程的跨进程连接。用 actor 而不是加锁全局字典，是为了不用 `@unchecked Sendable`（那等于把并发正确性从编译器手里拿回来自己保证） |
| 56 | DEBUG 里可以打识别结果（对隐私规则的一次刻意破例） | 包在 `#if DEBUG` 里，release 构建连代码都不存在；使用约定是只在开发机上写测试内容时看 | 不看识别出来的文字就无法判断识别对不对，而缺语言模型时识别器不返回空、会吐出看起来正常的垃圾——没有这一行，那种失败完全无声 |
| 54 | A10 报告自证数据来源 | 用「笔与屏幕的夹角有没有变化」判断是笔还是鼠标；不像笔就在报告里说重话 | 模拟器上只能用鼠标画，而鼠标的速度和抖动不是人手的。拿那批数字调手感等于把参数调到一个不存在的「人」身上，而且调完还以为校准过了。指望人记得「这次是鼠标」不可靠 |
| 45 | 最低系统（2026-08-29 用户拍板） | 部署目标 **iPadOS 26**，`PKStrokeRecognizer` 用 `#available(iOS 27)` 门控 | 三件待验证的事里只有识别需要 27；`TimelineView` 连续动画与真实 Apple Pencil 的停顿分布（A10 与 E3c 阈值的唯一依据）在 26 上就能测。**这不是放弃 27**：识别仍然要 27，只是不再拿它挡住不需要它的测量，用户也不必在唯一一台 iPad 上装 beta。已实测：App 在 iOS 26.5 上跑起来，页面如实显示「这台设备的系统还没有手写识别（需要 iPadOS 27）」 |
| 46 | 「读不出来」要分三种（E4b 扩展） | 系统太旧 / 有 API 但缺语言模型 / 有模型但认不出，三种分开报 | 解决办法完全不同：升级系统、换设备、写清楚一点。合成一个「不可用」会让用户按错方向去解决 |
| 47 | 校准结果不自动写回配置（A10 结论） | `HandwritingCalibration` 只输出「量出来的 vs 现在用的」，改不改由人决定 | 自动写回意味着一次异常采样就能悄悄改掉全 App 的手感，而且没人知道 |
| 48 | 量不出来的项不给默认值（A10 结论） | 返回「量不了 + 原因」，绝不给一个数 | 给了默认值，「已校准」就成了谎话。压感就是这种情况：这支笔没有压感，永远量不了 |
| 49 | C1 混写语言（2026-08-29 用户拍板） | 魂**跟着用户的语言习惯走**，不把中英混写翻译成纯中文 | 用户写 `review`，回应里出现 `review` 才自然；硬换成「评审」像换了个人在说话 |
| 50 | C1 必须含危机场景（2026-08-29 用户拍板） | 加第 11 段，且作为**否决题**单独看，不并入排名 | 日记是人在最低谷时会打开的东西。模型在这一段回避、或切成客服口吻，无论总分多高都不能用 |
| 44 | 重采样怎么铺点（A8 结论） | 把总长**等分成整数份**，不再按固定步长累加、最后硬补一个总长 | 老写法在总长刚好只比上一个点多一点时，末尾会留一段长度接近 0 的碎段。摆动要在那里算法线（方向退化）、渐细要在那里判位置、渲染要画一段看不见的线——都不报错，只让收笔偶发地不对劲。等分之后实际间距与请求值略有出入，这是对的：间距是「大约多密」的目标，端点精确与无退化段才是不能让步的 |
| 42 | 空格怎么表达 | 「有宽度、没有墨迹」的零笔画字形，不是缺字 | 报成缺字属**假警报**：界面会说「这一页少了字符」而实际什么都没少，还会让真正的缺字（生僻汉字）淹没在噪声里 |
| 36 | 抖动的形状（A1 结论） | 沿弧长的**连续摆动**，位移打在笔画法线方向；波长由生理性手抖频率（10 Hz）与书写速度推出，不单独拍数 | 逐点独立白噪声不是手抖，是让线条边缘起锯齿。而且因为难看，幅度只能压到几乎看不见，功能形同虚设。法线方向才会把线掰弯；沿切线推点只改变点的疏密，看不出来 |
| 32 | 成页之后没有回应时说什么 | 页面上一行直白的实话（「回应还没接上——魂尚未接入」），不编世界观台词 | 渗墨预告会让人期待回应。什么都不说像 bug，编一句「我在想…」是伪装成功。这行文案是开发期的，E6 接上后删除 |

已排除：位图路线（实现过、测试过、效果差，已删且不留死代码）；Python 位图/向量对比 spike；预制笔画母题库；图像生成与图像回应；Magic Stroke Lab 与离线夹具。

## 风险

| 风险 | 处置 / 状态 |
|---|---|
| 魔法体验不动人（生死项） | E7 手写文字回应的 Go/Kill 评审仍待做。E1 已完成，逐笔生长在 Xcode 预览里能看，因此**现在已经具备初判条件**；但连续动画的真实观感仍受模拟器停帧限制，正式评审要真机 |
| **App 没有本地化文件**（2026-08-29 发现） | 界面文案全是硬编码中文，没有 `.strings`/String Catalog。已把工程的 `developmentRegion` 从 `en` 改成 `zh-Hans`，否则 `Locale.current` 会解析成英文、语言名显示成 "Chinese" 而界面是中文。这只是让现状自洽，**不等于做了本地化**；真要出海必须补 String Catalog，届时所有硬编码文案都要搬 |
| 每抬一次笔就识别一次整页（已知代价，非 bug） | 这么做有原因：终止标点加速信号必须在成页**之前**就知道你写了句号，而识别是异步的，等到成页再识别永远来不及。代价是识别频率随页面变长而变贵。真机测出识别延迟后若太贵，就降级成只在成页时识别、同时放弃标点加速（属计划 B） |
| 成页阈值的倍数与上下限仍无真机依据 | 倍数 6、下限 1.5s、上限 8s、标点比例 0.45、悬停宽限 2.5s，全部只有一次模拟器鼠标书写作参照。真机 + Apple Pencil 的停顿分布拿到前，不得称阈值已校准 |
| **缺语言模型时识别器返回垃圾而非空**（2026-08-28 实测，新发现） | 模拟器缺中文模型时写「你好」，识别结果是 `15.47`——不是 nil，是一串它自信认出的拉丁/数字。危险在于：接上 Oracle 后，AI 会认真回应一串乱码，用户完全不知道为什么。**而且无法过滤**：没有中文模型就无从判断「这几笔本来是中文」。唯一诚实的做法是在用户书写**之前**就告知本机认不出中文。`RecognitionAvailability.unavailable` 已经把信号做出来了，但目前只显示在 DEBUG 读数里，必须真正用上（属 E4b） |
| **手感数值全部没有真人依据（贯穿 A 组与 A10）** | A1–A5、A7、A8 的**结构**已修完，其中 A3/A5/A7/A8 是零参数的（由人类运动模型、概念分离、墨的物理、数值稳定性推出）。但 A1/A2/A4 共七个数值仍只是量级推算。`HandwritingCalibration` 量尺已就位，能量出手抖频率、抬落笔耗时、空中速度、书写速度、手抖幅度；**压感相关两项永远量不了**（这支笔没有压感）。差的只是用户在真机上写几页字——部署目标已降到 26，这件事不再被挡着。在那之前不得称手感已验证 |
| 已解决，留一行避免误以为遗漏 | ~~成页时识别整页~~ ✅ 2026-08-29 按落笔时刻切轮次（E3e）。~~中文手写识别是头号风险~~ ✅ 2026-08-29 用户在 iPad 10 + iPadOS 26.6 + USB-C Apple Pencil（无压感）上用系统 Scribble 手写中英混写全部认对——**中文模型就在真机上、质量够用、不需要压感**。仍未验证的只剩 `PKStrokeRecognizer`（27 的 API）能不能用上同一套模型；两者几乎确定共用 `CoreHandwriting`，所以风险已降为一次接口确认 |
| 识别准确率的已知边界 | 拉丁字母**可用且质量不低**：模拟器鼠标写 `hello / how are you? / My Darling !` 与 `hi hello I loveyou!?` 基本全对（首字母大写是识别器的句首规整，不是错误）。中文在**模拟器上恒为坏的**：iOS 26.5 与 **27.0 模拟器都缺中文模型**（`supportedLanguages` 只报拉丁语言；根因是 `CoreHandwriting.framework` 里中文只有码表没有本地权重，非拉丁模型靠系统资产按需下载而模拟器不带）。此前「识别率不高」的观感全部来自中文那段。备选路线：Vision `RecognizeTextRequest` `.accurate` 支持 `zh`/`zh-TW`/`yue`，代价是丢掉笔画顺序且面向印刷体，仅在真机上 PencilKit 中文准确率不够时启用 |
| Scribble 不能直接拿来用（已判断，不是待办） | Scribble 是「往文本框里写字」：它会把笔迹**吃掉**变成文字。而本产品的核心是用户的手写必须**留在纸上**。所以 Scribble 只能当能力探针，不能当输入通道 |
| 备选识别路线 | Vision `RecognizeTextRequest` `.accurate` 实测支持 `zh`/`zh-TW`/`yue`/`yue-CN`（共 30 种；`.fast` 只有 6 种拉丁语，`supportedRevisions = [revision3]`）。做法是把 `PKDrawing` 渲染成图再识别，代价是丢掉笔画顺序，且 Vision 面向印刷体与照片文字。仅在真机上 PencilKit 中文准确率不够时启用 |
| 书写停顿分布（2026-08-28 首批真实测量） | 模拟器鼠标书写：**中位停顿 0.51s，最长 53.37s**，总时长 111.2s 而落墨仅 27.3s（76% 时间在停顿）。中位停顿与「写完了」的停顿差两个数量级，说明用停顿长度判断成页可行。局限：鼠标比真笔慢，且 53s 那次很可能是在截图而非「写完了」，所以是**指示性数据不是结论**；E3c 的阈值仍须真机 + Apple Pencil 采样。这也解释了原「2.8 秒」为何会误触发——书写过程本就充满停顿 |
| **E9c 的字号估算目前不能用来决定魂的字号**（2026-08-31 实测暴露） | 用户在模拟器写了一页中英混写 + 涂鸦，实际字高约 90 点，估出 251.5 点，**偏 2.5 倍**。根因：0.49 那个换算比例只对汉字成立——人写拉丁字母时一笔常跨过整个字高（比例接近 1），涂鸦也被当成很高的字。现改为报告同时给出原始跨度并注明只作诊断。已想到的修法是用「每字几笔」区分文字（汉字五到十几笔一字、拉丁一两笔一字，成页时两个数都现成），但它依赖识别可用，而模拟器上中文识别本身就是坏的，所以现在验不了 |
| **手写定义的字形没有上游可比对**（E1c/E1d，2026-08-29） | 27 个标点 + 62 个拉丁字母数字的形状由我按印刷体常见样子写出。可机器验证的部分有测试守着（覆盖完整、落在字宽内、基准线、笔画不退化），但「像不像那个字」只能靠眼看——已把全部字形渲染成字符画到 `/tmp/tomriddlesdiary-{punctuation,latin}.txt` 并人工核对。最没把握的一处是**顿号 `、` 的倾斜方向**，已在代码里单独标注 |
| **中文断行规则仍然没有**（2026-08-29 新暴露） | 标点补上之后这条缺口才真正显形：句号可能出现在行首、「」可能被拆到两行、行尾的空格没有折叠。CoreText 会做这件事，但它的比例字宽模型与等宽字面方格不兼容，所以 `GlyphStrokeLayout` 里没有 |
| 压感数据无来源（已实测确认，2026-08-28） | 模拟器鼠标写字实测 `PKStrokePoint.force` 精确为 **0.000…0.000**（不是恒定非零值），`hasVaryingForce` 正确报 false。USB-C 款 Apple Pencil 无压感传感器，第三方笔在 iPad 上也拿不到压感（Logitech Crayon、Zagg Pro Stylus 官方明确不支持；普通蓝牙笔即使配对 iPadOS 也不给压感与倾斜；个别宣称有压感的只在集成其厂商 SDK 的 App 内有效，不经 PencilKit）。**结论：对当前硬件「没有压感」是常态而非例外。** 引擎自己生成压感所以照常工作，但 A10 的力度维度与 C2 的力度信号除非补一支 Apple Pencil 一代（用户已决定不补），否则永远拿不到数据。速度、停顿、涂改次数、字大小不受影响 |
| 用户的字与魂的字质感不统一（2026-08-28 实测发现） | 截图对比：用户笔画细而均匀（0.5 mm 固定宽、无压感），魂的回应字有明显粗细变化（字体自带字形）。同一页上两者不像同一支笔写的。原因一半是输入无压感，一半是 `userInkWidthInMillimeters` 可能偏细。待真机 + 有压感的笔后重新评估 |
| 永久 Key 泄露 | 见 `AGENTS.md` 红线；本地 DEBUG 例外须最小权限+额度+监控 |
| 无证据承诺“不出境/读完即删” | 设计目标与供应商事实分开，逐项验证 |
| 内部 IP 占位进入公开面 | 门禁 + staged diff 人工审查；分发前原创化。注意：2026-08-26 起 `AGENTS.md` 的「IP 与合规」节已被用户删除，禁用词只剩脚本层，发布前清单（年龄分级 / AI 披露 / 自残危机兜底 / 删除全部数据 / 跨境复核）目前无文档承载，待用户决定是否恢复 |
| 字形笔顺数据许可 | makemeahanzi / hanzi-writer-data 派生自 Arphic 1999 公共授权字体，分发前必须复核授权义务；本地非分发实验不受此阻塞 |

已接受但受限：项目名、仓库名、占位 bundle ID、本地 persona 只限私有非分发面，不代表商业授权。

## 模型与 Key 边界（2026-07-23 核对官方说明）

- Key 按区域区分；部分区域（北京、新加坡、东京、法兰克福等）调用需把 Workspace ID 放进 Base URL，接入时以实际账号页面为准。
- 移动端正式调用经安全后端；官方建议由后端为不可信环境签发临时 Key（默认 60 秒，可配 1–1800 秒，继承签发 Key 权限）。
- 参考：[Qwen 首次 API 调用](https://help.aliyun.com/en/model-studio/first-api-call-to-qwen)、[临时 API Key](https://help.aliyun.com/en/model-studio/application-obtain-temporary-authentication-token)。
- 未核实：模型实时可用性/配额/价格、大陆到端点的可达性与延迟、供应商对请求/日志/备份/训练/删除的条款、"数据不出境"的合同与全链路证据、自部署版本许可证与运维责任。

## 待用户决定 / 待用户执行

1. **E6（Oracle + Mock 闭环）是现在的下一个大步。** E9 五步全部完成，落点已不再挡路；E3f（写满翻页）的判据（找不到空位 → `noRoomOnThisPage`）也已就位。**待用户确认两件**：DEBUG 的识别结果输出可否保留；`Evaluation/` 算不算第四个文档目录（当前三份文档职责表里没有它）。
2. **真机验证（最要紧）**：iPad 10 支持 iPadOS 27。装上后要验三件——中文手写识别是否可用（决定整条输入端）、`TimelineView` 连续动画是否正常（决定逐笔重播能不能做）、真实书写的停顿分布（决定 E3c 的成页阈值）。三件在模拟器里都验不了。
3. **画布输入策略**：`InteractionSettings.drawingPolicy` 暂取 `.anyInput`（手指也能写）。要不要只认 Apple Pencil？只认笔的话按 AGENTS.md 必须明确告知，不能让用户对着无反应的纸猜。
4. 行距 1.6 倍字高、页边距 8% 短边、用户笔宽 0.5 mm，都是量级推算，真机观感待复核。
4. 正式 bundle ID、签名主体、原创公开品牌与 Persona 方向；何时购买 Apple Developer Program（beta 期间无法分发，正式版约九月）。
5. `AGENTS.md` 被删除的发布前安全清单（自残危机兜底、AI 披露等）——用户已明确不恢复，此处仅留记录以免将来误以为遗漏。

## 用户偏好

- 中文交流，大白话解释术语，需要类比与详细但易懂的说明。
- 区分「已验证事实 / 合理假设 / 待用户决定」，不混写。
- 不替用户拍板命名、bundle ID、品牌、付费与架构方向。
- 不静默兜底：阻塞、测试失败、远端分叉必须暴露。
- 用户会明确要求「先不要进行下一步」，必须停住等确认。
- 重视真实可演示，不接受把命令 exit 0 当成体验通过。
- 每次实质任务收口自动执行安全 Git 同步。

## Agent 自身的判断错误（记下来避免重犯）

- **2026-08-27：把「模拟器返回了什么」当成「平台支持什么」。** 只依据模拟器上 `supportedLanguages` 返回 20 种拉丁语言，就断言 PencilKit 不支持中文，还据此回滚了最低系统。用户翻出官方 session 的支持语言列表才纠正。教训：运行时能力查询反映的是**当前环境装了什么**，不是平台契约；平台契约要看官方文档。两者不一致时先找原因，不要拿环境读数推翻文档。
- 同一错误的第二个表现：结论下成了「输入端整体换路」，而正确结论应是「拉丁字母照用，只有中文这一格待验证」。能力缺失要按语言/按功能定位到最小范围，不要放大成整条路线报废。

## 已知陷阱

- **模拟器空闲时冻结渲染循环**（2026-08-27 实测）：`TimelineView` 的 `.animation` 与 `.periodic` 两种调度在模拟器里都只出一两帧就停，画面卡在第一个字。由 `@State` 变化触发的重绘照常生效。因此 E8 的逐字显现改成异步循环推进状态。**连带后果：`HandwritingReplayView` 的逐笔重播用的是 `TimelineView`，它的连续动画在模拟器里无法验证，必须真机确认。** 不要因为模拟器里不动就以为代码错了——这和中文识别模型缺失是同一类环境限制。

- **两个配置常量之间有隐藏耦合**（2026-08-28 被测试撞出来，不是事先想到的）：成页倒计时每 0.1 秒轮询一次，而最短的预告期是 `shortestWait × hintFraction`。后者一旦短于前者，一次轮询就跨过整个预告期，「可撤销的预告」直接消失，症状只是「提示时有时无」，不报错也没人会想到去查那个间隔。已加断言 `PageCommitTriggerTests.testHintWindowOutlastsTheCheckInterval` 守住。
- **浮点累加顺序会让「刚好画完」差一个尾数**（2026-08-29 被端到端用例抓到）：时间轴算落墨终点时若先 `cursor += pauseBefore` 再比 `cursor + duration`，累加顺序就与 `StrokeSequence.totalDuration` 不一致；笔数一多，`frame(at: totalDuration)` 会差出尾数、最后一笔差一点点画不完——线尾少一小截，没有任何报错。凡是「累加出来的总量」和「逐步推进的游标」要对得上的地方，两边必须用同一种累加方式。
- **两条来自系统的日志噪声，不是 bug**（2026-08-31 记下，免得下次当成故障查）：
  一、`Invalid reflowable tokens count` / `Skipping adding principal lines to token` 来自系统的手写守护进程 `handwritingd`，是 `PKStrokeRecognizer` 内部的日志，与本工程无关。
  二、`UIKitHoverGestureRecognizer ... has been in possible phase for N seconds` 是 UIKit 的看门狗警告，指向我们那个只接受 Apple Pencil 触摸类型的悬停识别器。当前所有可用硬件（模拟器、iPad 10）都不支持悬停，所以它永远不会开始，就一直停在 possible 阶段。属预期，不是泄漏。
- **`@Observable` 不比较新旧值**（2026-08-29 踩到，代价是一个真 bug）：把同一个值再写一遍，观察者照样收到通知、界面照样重建。倒计时每 0.1 秒写一次 `phase`（大多数次值没变），界面每秒重建十次，PencilKit 的墨迹层被反复标记为需要重绘却一直没重绘——**用户写的字从纸上消失了**，而数据全在、纸色也在。凡是被高频调用的地方赋值给 `@Observable` 属性，都要先比较。
- Swift 6 下**不能**给 `XCTestCase` 子类整体标 `@MainActor`（与继承来的 nonisolated 初始化方法冲突，直接编译报错）。要测 `@MainActor` 的类型时，隔离标在具体测试方法上。
- Xcode 26.6 并行 test clone 触发 `DVTiPhoneSimulator` assertion → 测试关闭并行。
- Xcode 曾自动建嵌套 Git，已可恢复地移到 `.git/xcode-nested-repo-backup-8d5eca1`。
- 工程用 objectVersion 77 同步文件组：`Sources/`、`Tests/` 下新增文件自动入 target，不需手改 pbxproj。
- kiro-pet 已迁至 `110_KiroPet`，不得放回本仓库。

## 历史（精简）

<!-- 只留「以后还会影响判断」的结论。过程细节由 Git 历史承载，不在这里重复。 -->

- **2026-07-15 至 08-27（起步、地基、输出侧脚手架）**：仓库骨架、规则/记忆/脚本、GitHub 私有远端、Xcode 工程纳入根仓库、修正为仅 iPad。建立单位契约与集中配置（D1–D6：只有字高用毫米，其余全是字高的比例；时长不变量进类型；改单调时钟）。删掉位图路线与 Magic Stroke Lab，文档收敛为三份。用户拍板首个产品目标「手写文字进 → 手写文字出」。打包寒蝉手拙体 + CoreText 断行（E2/E8），第一次看到回应出现在纸上。**这一段留下三条还在生效的教训**：一、我曾把「模拟器缺中文模型」判成「平台不支持中文」并据此回滚了 iOS 27，被用户用官方文档纠正——运行时能力查询反映的是环境装了什么，不是平台契约。二、排版失败时返回 nil 让纸上空白无提示，那正是我自己要防的静默兜底，改成显式三态。三、模拟器空闲会冻结渲染循环，`TimelineView` 停帧，改用状态驱动。
- **2026-08-28（输入端接通 + 字形笔画 + 成页与打断）**：升 Swift 6 并给纯逻辑层加 import 白名单门禁（当场应验：编译器直接挑出 `PencilStrokeReader` 不该绑主线程）。接上 PencilKit 画布，引擎第一次有真实输入源；压感刻意不伪造。E1：`graphics.txt` 30.78 MB 只留 medians 压到 5.26 MB（9574 字），`GlyphStrokeLayout` 按字面方格等宽排版，刻意**不复用 `TextLayout`**（字体是比例字宽、字形数据是等宽方格，混用会让字互相压）。E3c 最重要的一条：**成页阈值不再是固定秒数**，改成本页停顿中位数 × 倍数——实测书写本身 76% 的时间在停顿（中位 0.51s，最长 53s），固定秒数对写快和写慢的人必有一方是错的（原「2.8 秒」就是这样误触发的）。E3d 把渲染层入参从 `Instant?` 改成 `ReplayPlayback` 三态：原契约表达不了「停在当时的进度」，只能被迫映射成「画完」，正好把「你打断了它」这个信息抹掉。同轮撞出一个重要事实：**缺语言模型时识别器返回垃圾而不是 nil**（「你好」→`15.47`），事后无从过滤。
- **2026-08-29（修自己造的 bug + E3e + E4b + A1）**：用户写的笔画写完就从纸上消失——根因是倒计时每 0.1 秒往 `phase` 写值且值没变也写，`@Observable` 照样发通知，界面每秒重建十次，PencilKit 墨迹层被反复标脏却因模拟器冻结渲染而没重绘。改成只在真变化时发布，一轮从几十次降到 4 次，加回归守卫。E3e 按落笔时刻切轮次（同时纠正我此前「这取决于版式决定」的错判——版式管坐标，轮次只管时间）。E4b 落笔前就查识别能力并如实告知；过程中发现工程 `developmentRegion` 是 `en` 导致语言名显示成 "Chinese"，改成 `zh-Hans` 让现状自洽，并记下「没有本地化文件」这个真实缺口。A1 抖动改成沿弧长的连续摆动、位移打法线方向，波长由手抖频率与书写速度推出。
- **2026-08-29（A 组手写观感 + 手写字形补齐 + C1 评测集）**：A2–A5、A7、A8 全部完成，其中 A3（最小急动度轨迹）、A5（压感与接触分离）、A7（√进度）、A8（等分重采样）都是**零新增参数**——分别由人类运动模型、概念分离、墨的物理、数值稳定性推出，没有拍板余地。A5 让收笔第一次真的收到零宽。E1c/E1d 手写 27 个标点 + 62 个拉丁字母数字：先建 `PenMove` 词汇让每一笔读得出意图，再把全部字形渲染成字符画人工核对（没有上游可比对，这是唯一形状核对手段）。被测试撞出三个真问题：浮点累加顺序让最后一笔差尾数画不完、空格被报成缺字（假警报）、墨点与线段对 infinity 处理不一致。用户实测 Scribble 排除了中文识别这个头号风险。
- **2026-08-30（降到 iPadOS 26 + 清脚手架 + A10 量尺 + E9a–E9d）**：按用户决定部署目标降到 26，识别用 `#available(iOS 27)` 门控，`RecognitionAvailability` 把「系统太旧」与「缺语言模型」分成两种话说。顺带发现测试套件一个真缺口：6 条识别用例假设识别器一定存在，在 26 上全红——改成 `XCTSkip` 显式跳过而不是塞空分支（空分支显示「通过」，那是假绿）。清掉全部开发期脚手架，纸上只留 E4b 那句真提示。新建 `Calibration/`：量出手抖频率/抬落笔耗时/空中速度/书写速度/手抖幅度，**量不了的项返回「量不了 + 原因」而绝不给默认值**，报告并排显示「量出来的 vs 现在用的」且**不自动写回配置**；再加「数据来源」自证（用笔与屏幕的夹角是否变化区分笔和鼠标）。E9a–E9d 建起落点决策的全部输入，其中最有价值的一步是字号换算比例：原本单笔跨度直接当字高用，实测系统性偏小一半，于是拿打包的字形数据当标准答案量出 0.49，并加测试从真实数据重新量一遍断言——把一个会静默出错的猜测换成被机器盯着的测量值。
- **2026-08-31（用户模拟器实测暴露三件事 + E9e 收口）**：实测确认墨迹不再消失、成页与识别链路跑通、A10 的「数据来源」自证正确判成鼠标而非真笔（`penLiftDuration` 拟合出负数时也如实报「量不了」，守卫生效）。同一次测试暴露三件并当轮处理：一、**我上一轮的回归**——为绕开部署目标降级带来的类型可用性问题，我把系统识别器改成每次新建，理由是「缓存的好处从未测量过」；实测证据来了，30 笔刷出约六十条 `Remote connection to handwritingd was invalidated`，已用 `@available(iOS 27) actor` 恢复缓存。二、**字号估算偏 2–2.5 倍**，根因是 0.49 只对汉字成立（人写拉丁字母时一笔常跨过整个字高），已改为同时报告原始跨度并标注「只作诊断」。三、加一行 DEBUG 专用识别输出（release 里代码都不存在）——不看认出来的文字就无法判断识别对不对。**并确认 iPadOS 27 模拟器仍然缺中文模型**（`可用[en] 缺失[zh]`，英文认得准），所以 `PKStrokeRecognizer` 的中文仍只能等真机。然后完成 E9e：`ReplyPlacementFinder`。这一步最值得记的是**第一版是错的且错得很典型**——我把「回应与用户笔迹隔多远」做成一个凭手感取值的参数（0.3 字高），结果右边和下方两个候选几乎永远失败、回应总掉进扫描退路；根因是同一条「别贴着写」的规则被写了两遍（这里一遍、占用图的 `clearance` 一遍），撑开量恰好把间距吃回去。改成从占用图推出（`clearance + cellSize`）之后落点决策只剩两个真正要人定的数。同时把 `SeededRandomNumberGenerator` 从 `StrokeHumanizer` 提到 StrokeEngine 共用（复制第二份会让两处实现分叉，而分叉的症状是所有手感回归测试同时失去意义）。284 测试在 iOS 26.5 与 27.0 上都通过。
