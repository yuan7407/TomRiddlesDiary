<!-- 动态状态与决策。会话开始或用户说“继续”时先读本文件。规则见 AGENTS.md，项目说明与计划见 README.md。保持 ≤120 行。 -->
# 项目记忆

## 当前状态

- **阶段**：输入端整条链路已通（E3a→E3b→E3c→E3d→E4 接线），输出端逐笔生长已通（E1a/E1b + E2 + E8）。计划编号见 `README.md` 的 A–H 表；字母是名字，执行先后见那里的「执行顺序」。
- **分支**：`main` 与 `feature/magic-stroke-lab` 两条，内容相同。后者名字已与内容不符（它最终删掉了 Magic Stroke Lab），后续开名字对得上的新分支（F10）。
- **链路上唯一缺的一环是 Oracle（E6）**：用户写字 → 停笔 → 渗墨预告 → 成页 → 识别出文字 → **断在这里**。文字拿到了，没有东西能把它变成回应。接上之后 `DiaryPageModel.beginReply` 就是入口，后面的排笔画、手绘化、逐笔重播、落笔中断全部已就绪并可运行。
- **App 跑起来的真实样子**：一张能写字的纸；停笔后左上角渗墨（预告，再写一笔即取消）；不写就成页并识别；成页后页面**如实说明魂还没接入**，不假装在思考。DEBUG 读数在左下角，显示笔数、停顿、算出来的成页阈值、当前阶段、识别结果。
- **引擎**：`StrokePipeline → StrokeHumanizer → StrokeReplayTimeline`，入口只吃 `[Polyline]`（有笔顺的有序坐标）。两个真实生产者都已就位：`PencilStrokeReader`（用户手写）与 `GlyphStrokeLayout`（字形笔顺）。
- **渲染层**：`Features/Response/HandwritingReplayView`，入参是 `ReplayPlayback` 三态（在播 / 停在某进度 / 已写完）。坐标 1:1，不缩放。
- **单位契约（D1/D2 已完成）**：坐标一律页面点；**只有字高用毫米声明**（默认 9 mm ≈ 46.8 pt），间距、抖动、速度、墨宽全部是字高的比例。`Configuration/` 下四个文件是经验性常量的唯一定义处：`PageMetrics`（唯一毫米↔点换算）、`HandwritingFeel`（手感 + 装配入口）、`PageAppearance`（纸墨与说明文字浓度）、`InteractionSettings`（怎么算一次输入 + 成页参数）。引擎已无默认参数。
- **验证**：124 个 XCTest 全通过；Simulator build 成功；空白页截图人工确认。测试必须加 `-parallel-testing-enabled NO`。
- **未接入**：Oracle、任何网络或密钥、加密存储、后端、TestFlight。标点与拉丁字母的字形笔画（E1c/E1d）。
- **目录**：`Sources/` 下为 `Configuration / Features(Canvas·Response) / Handwriting / StrokeEngine / Resources` + 入口。

## 决策表

| # | 决策 | 结论 | 为什么 |
|---|---|---|---|
| 1 | 核心交互 | 手写文字，不做文字聊天复制品；涂鸦识别往后排 | 笔画与逐笔节奏是差异化内核 |
| 2 | 手感来源 | 本地引擎负责，不靠模型输出最终笔画 | 模型笔画不稳定，节奏与压感必须可控 |
| 3 | 笔画源 | 引擎只吃有序向量 `[Polyline]` | 位图抽骨架实机效果差且已删；手打夹具也已删。代价是当前零个生产者，两个真实来源（字形笔画中线、PencilKit 手写）都待建 |
| 4 | 模型通道 | 视觉/文本模型藏在 `OracleProvider`/`OracleRouter` 后，具体供应商不写进业务代码 | 避免供应商锁定；DeepSeek 官方 API 只有实验性图像**理解**（`deepseek-v4-flash-vision-exp`），没有文生图，所以「模型出笔触」在事实层面不成立 |
| 5 | 模型鉴权 | 本地非分发 DEBUG 可受限实验；分发前必须安全后端 | `.gitignore` 挡不住客户端逆向 |
| 6 | Persona | 内部占位可替换、环境门控；公开版原创 | 体验探索与 IP 风险隔离 |
| 7 | Apple 账号 | 现在用 Simulator/免费账号 | 当前不需要 TestFlight |
| 8 | bundle ID | Xcode 占位值，非正式决定 | 主体、域名与品牌未定，须用户拍板 |
| 9 | 规则源 | 根 `AGENTS.md` 唯一 | 避免多份规则漂移；已删除 `CLAUDE.md` 指针文件 |
| 10 | 文档层级 | `AGENTS.md`(规则) + `README.md`(说明/计划) + `MEMORY.md`(状态) | 三份各有唯一职责，不再有 topics 分散 |
| 11 | 首个产品目标（2026-08-26 用户拍板） | 手写文字进 → 手写文字出：用户用 Pencil 手写文字，一段时间后收到逐笔手写的文字回复。图像理解与图像回应往后排 | 绕开「像素→笔顺」这个已证明很难的问题；文字这条腿的桥是字形笔顺数据，不需要矢量化 |
| 12 | 预制笔画母题库 | 不考虑 | 组合太少、方法太笨（用户判断） |
| 13 | 断网/失败降级 | 现阶段只做诚实硬提示，不做假对话；世界观内的断网动画留待以后 | 魔法要完整才有意义，半成品假对话比硬提示更伤 |
| 14 | 重播打断 | 用户新落笔即中断并接管，半截字留在页上不回退 | 逐笔重播不能是不可打断的单向表演；半截字最诚实 |
| 15 | 手写识别路线 | 走 iPadOS 27 的 PencilKit 内建识别（`PKStrokeRecognizer`）。**决定不变** | Apple 自家、免费、离线、隐私最好、不花 token。官方支持中文简繁与粤语。模拟器缺中文模型只是环境限制，不是平台限制 |
| 16 | 图像回应 | 砍掉，不是暂缓。相关素材规格、占位 key、管道描述全部删除 | 已废逻辑链留着会持续误导 |
| 17 | 成页触发 | 长停顿为主 + 终止标点加速 + 等待期可撤销视觉预告 + Pencil 悬停（有则用之）。阈值必须由真实书写数据量出 | 无按钮；漏触发比误触发严重；猜错必须零代价可救 |
| 18 | 魂的笔画归属 | 用户可以擦掉。动画播完后笔画落定进 `PKDrawing`，与用户笔画同一份数据 | 否则 PencilKit 橡皮擦不到，自己实现橡皮会重复系统能力且手感不一致 |
| 19 | 渲染层去留 | 不整体删，删 60% 保 40%（搬家瘦身） | 分段绘制与半段插值是逐笔生长的实现核心，且不会因 B1 作废；自动缩放与卡片外观才是错的 |
| 20 | 主界面按钮 | 写字那张纸上零按钮；设置、隐私、删除数据从页边缘划出 | 魔法不能有按钮，但 AGENTS.md 要求的删除数据路径必须可达 |
| 21 | 输出侧分两阶段（2026-08-27 用户拍板） | 先用手写体字体跑通链路（E8），再引入字形笔画数据实现真正逐笔生长（E1）。字形笔画是确定要做的，不是备选 | 字体版本能立刻验证排版与时序且不依赖任何升级；但字体是轮廓不含笔顺，给不出逐笔生长，所以只是脚手架，不能用它判生死（E7 必须等 E1） |
| 22 | 最低系统 | iPadOS 27.0（已落地）。构建必须用 Xcode 27（命令行用 `DEVELOPER_DIR` 指定，不改全局 `xcode-select`） | PencilKit 内建识别只有 27 有。代价：beta 期间无法上 TestFlight/App Store，正式版约九月 |
| 23 | 设备范围 | 现阶段仍 iPad-only（`TARGETED_DEVICE_FAMILY = 2`）。iPhone / 折叠机等真机存在且有对应设计后再谈 | 折叠 iPhone 未发布，规格未知；且没有任何 iPhone 支持 Apple Pencil，手指在手机上写汉字是另一种交互。现在打开 iPhone 安装只会让核心功能不可用，属伪装可用。真正的「兼容更多设备」靠 D1 的比例化参数与零硬编码尺寸达成，不是靠翻一个开关 |
| 24 | 书写方向 | 横排竖排都要兼容。输出侧由排版层的方向参数决定，代价小；**输入侧竖排识别是未知**，已加入 F3 门禁的检查项 | 用户要求两者兼容；但识别对中文竖排的支持无法从外部确认，必须实测 |
| 25 | 参数单位（D1 结论） | 只有字高用毫米，其余全是字高的比例；时长用绝对秒 | 真人写小字时手抖幅度也更小，绝对毫米会把小字糊掉；时间不随字号变化 |
| 26 | 手写体字体（2026-08-27 用户拍板） | 寒蝉手拙体 v2.500，中英文都用它 | 作者 README 明确「免费授权全社会使用（包括商用）」，请求标注作者为寒蝉字型。实测拉丁与汉字都是不等宽比例字宽（A=490/a=314/我=858/的=631，每 em 1000），比等宽更像手写。GB2312-80 字符集、7586 字形；**生僻字会缺字**，届时会显示豆腐块 |
| 27 | 排版实现 | 用 CoreText 断行定位，不自己按格子排 | 这个字体连汉字都不等宽，按格子排会挤或散；中文换行规则（标点不能在行首等）自己写必然漏 |
| 28 | 字形笔顺数据授权 | Arphic 1999 版 | 唯一实质区别是商用许可，1999 允许商用；2010 版并非更全更强，只是更严格。个人非商用用 1999 已足够 |
| 29 | 成页阈值怎么定（E3c 结论） | **不是固定秒数**：阈值 = 本页笔间停顿的中位数 × 倍数，上下限只作护栏 | 实测一页字里 76% 的时间在停顿、中位停顿 0.51 秒，任何固定秒数对写快的人和写慢的人必有一方是错的（原「2.8 秒」就是这样误触发的）。倍数随用户自己的节奏缩放，是唯一有物理含义的参数 |
| 30 | 量不出节奏时等多久 | 取**上限**（8 秒），不取下限 | 只写了一笔时没有笔间停顿可测。漏触发（写完了永远没反应，且没有按钮可救）比早触发严重得多，所以宁可多等——但因为有上限，它终究会响 |
| 31 | Pencil 悬停的地位 | 只能**有界延长**等待，绝不能阻止成页；硬件不支持时不做任何补偿 | 悬停只在 M2 及更新的 iPad 配 Apple Pencil Pro/二代 上可用（[官方规格](https://support.apple.com/en-us/111889)），模拟器和 iPad 10 都没有。它是加分信号，主信号不依赖它，因此「没有悬停」不是需要兜底的降级 |
| 33 | 页面写满怎么办（2026-08-29 用户拍板） | **翻页 + 翻页动画**，另加 DEBUG 提示。属计划 E3f | 静默不回应或把回应压在别人字上都不可接受。依赖回应落点先实现——「写满」的判据是「找不到放得下回应的空位」 |
| 34 | 「这一轮写了什么」怎么切（E3e 结论） | 按**落笔时刻**切：成页后记下分界点，之后只有更晚落笔的算新内容 | 不按笔画数量切：擦掉中间一笔再补写一笔数量不变，新内容会被无声漏掉。**纠正一次此前的错误判断**：我曾说这取决于版式决定，其实两件事无关——版式管坐标，轮次只管时间 |
| 35 | 模型评测集是文字还是手写图（C1 澄清） | **文字**。8–10 段你会写进日记的话（直接打字）+ 期望回应 | Oracle 只收得到文字，识别是另一条链路。混在一起会让「哪个模型更懂情绪」的结论被识别错误污染。手写样张属识别准确率测试（E4），是另一件事 |
| 36 | 抖动的形状（A1 结论） | 沿弧长的**连续摆动**，位移打在笔画法线方向；波长由生理性手抖频率（10 Hz）与书写速度推出，不单独拍数 | 逐点独立白噪声不是手抖，是让线条边缘起锯齿。而且因为难看，幅度只能压到几乎看不见，功能形同虚设。法线方向才会把线掰弯；沿切线推点只改变点的疏密，看不出来 |
| 32 | 成页之后没有回应时说什么 | 页面上一行直白的实话（「回应还没接上——魂尚未接入」），不编世界观台词 | 渗墨预告会让人期待回应。什么都不说像 bug，编一句「我在想…」是伪装成功。这行文案是开发期的，E6 接上后删除 |

已排除：位图路线（实现过、测试过、效果差，已删且不留死代码）；Python 位图/向量对比 spike；预制笔画母题库；图像生成与图像回应；Magic Stroke Lab 与离线夹具。

## 风险

| 风险 | 处置 / 状态 |
|---|---|
| 魔法体验不动人（生死项） | E7 手写文字回应的 Go/Kill 评审仍待做。E1 已完成，逐笔生长在 Xcode 预览里能看，因此**现在已经具备初判条件**；但连续动画的真实观感仍受模拟器停帧限制，正式评审要真机 |
| ~~成页时识别整页~~ | ✅ 2026-08-29 已修（E3e）。按落笔时刻切轮次。当时说「取决于版式决定」是我判断错了，两件事无关 |
| **App 没有本地化文件**（2026-08-29 发现） | 界面文案全是硬编码中文，没有 `.strings`/String Catalog。已把工程的 `developmentRegion` 从 `en` 改成 `zh-Hans`，否则 `Locale.current` 会解析成英文、语言名显示成 "Chinese" 而界面是中文。这只是让现状自洽，**不等于做了本地化**；真要出海必须补 String Catalog，届时所有硬编码文案都要搬 |
| 每抬一次笔就识别一次整页（已知代价，非 bug） | 这么做有原因：终止标点加速信号必须在成页**之前**就知道你写了句号，而识别是异步的，等到成页再识别永远来不及。代价是识别频率随页面变长而变贵。真机测出识别延迟后若太贵，就降级成只在成页时识别、同时放弃标点加速（属计划 B） |
| 成页阈值的倍数与上下限仍无真机依据 | 倍数 6、下限 1.5s、上限 8s、标点比例 0.45、悬停宽限 2.5s，全部只有一次模拟器鼠标书写作参照。真机 + Apple Pencil 的停顿分布拿到前，不得称阈值已校准 |
| **缺语言模型时识别器返回垃圾而非空**（2026-08-28 实测，新发现） | 模拟器缺中文模型时写「你好」，识别结果是 `15.47`——不是 nil，是一串它自信认出的拉丁/数字。危险在于：接上 Oracle 后，AI 会认真回应一串乱码，用户完全不知道为什么。**而且无法过滤**：没有中文模型就无从判断「这几笔本来是中文」。唯一诚实的做法是在用户书写**之前**就告知本机认不出中文。`RecognitionAvailability.unavailable` 已经把信号做出来了，但目前只显示在 DEBUG 读数里，必须真正用上（属 E4b） |
| 中文手写识别只能真机验证（头号风险） | 平台官方支持中文（WWDC session 203 的支持语言列表含简繁与粤语），但 iOS 27.0 beta 6 **模拟器缺中文模型**：`supportedLanguages` 只报 20 种拉丁语言，请求 `zh-Hans` 得到空数组，改系统语言与装中文手写键盘后仍不变。根因是 `CoreHandwriting.framework` 里中文只有码表 `ctc_zh.codemap`、没有本地模型权重（只有 `ctc_latn.bundle` 有），非拉丁模型靠系统资产按需下载而模拟器不带。**因此拉丁字母可在模拟器跑通，中文可用性与准确率必须等真机；在那之前不得声称中文识别已验证。** 代码必须如实处理「本机没有该语言模型」，不得假装识别成功 |
| 备选识别路线 | Vision `RecognizeTextRequest` `.accurate` 实测支持 `zh`/`zh-TW`/`yue`/`yue-CN`（共 30 种；`.fast` 只有 6 种拉丁语，`supportedRevisions = [revision3]`）。做法是把 `PKDrawing` 渲染成图再识别，代价是丢掉笔画顺序，且 Vision 面向印刷体与照片文字。仅在真机上 PencilKit 中文准确率不够时启用 |
| 英文识别准确率（2026-08-28 实测，好于预期） | 模拟器鼠标手写 `hello 你好 / how are you? / My Darling !`，认出 `hello15.47 How are you? My Darling I`。**英文除一个感叹号被认成 I 之外全对**；`15.47` 是「你好」的位置（中文模型缺失）。首字母大写是识别器语言模型在做句首规整，不是错误。结论：拉丁字母识别可用且质量不低；此前「识别率不高」的观感来自中文那段 |
| 书写停顿分布（2026-08-28 首批真实测量） | 模拟器鼠标书写：**中位停顿 0.51s，最长 53.37s**，总时长 111.2s 而落墨仅 27.3s（76% 时间在停顿）。中位停顿与「写完了」的停顿差两个数量级，说明用停顿长度判断成页可行。局限：鼠标比真笔慢，且 53s 那次很可能是在截图而非「写完了」，所以是**指示性数据不是结论**；E3c 的阈值仍须真机 + Apple Pencil 采样。这也解释了原「2.8 秒」为何会误触发——书写过程本就充满停顿 |
| 手感数值仍无真人参照 | 现有全部手感数值都只是按量级推算或早期肉眼调过，**没有任何测量依据**；`HandwritingFeel` 文件头已如实标明。A10 用真机笔迹替换前，不得称手感已验证 |
| 压感数据无来源（已实测确认，2026-08-28） | 模拟器鼠标写字实测 `PKStrokePoint.force` 精确为 **0.000…0.000**（不是恒定非零值），`hasVaryingForce` 正确报 false。USB-C 款 Apple Pencil 无压感传感器，第三方笔在 iPad 上也拿不到压感（Logitech Crayon、Zagg Pro Stylus 官方明确不支持；普通蓝牙笔即使配对 iPadOS 也不给压感与倾斜；个别宣称有压感的只在集成其厂商 SDK 的 App 内有效，不经 PencilKit）。**结论：对当前硬件「没有压感」是常态而非例外。** 引擎自己生成压感所以照常工作，但 A10 的力度维度与 C2 的力度信号除非补一支 Apple Pencil 一代（用户已决定不补），否则永远拿不到数据。速度、停顿、涂改次数、字大小不受影响 |
| 用户的字与魂的字质感不统一（2026-08-28 实测发现） | 截图对比：用户笔画细而均匀（0.5 mm 固定宽、无压感），魂的回应字有明显粗细变化（字体自带字形）。同一页上两者不像同一支笔写的。原因一半是输入无压感，一半是 `userInkWidthInMillimeters` 可能偏细。待真机 + 有压感的笔后重新评估 |
| 永久 Key 泄露 | 见 `AGENTS.md` 红线；本地 DEBUG 例外须最小权限+额度+监控 |
| 无证据承诺“不出境/读完即删” | 设计目标与供应商事实分开，逐项验证 |
| 内部 IP 占位进入公开面 | 门禁 + staged diff 人工审查；分发前原创化。注意：2026-08-26 起 `AGENTS.md` 的「IP 与合规」节已被用户删除，禁用词只剩脚本层，发布前清单（年龄分级 / AI 披露 / 自残危机兜底 / 删除全部数据 / 跨境复核）目前无文档承载，待用户决定是否恢复 |
| 字形笔顺数据许可 | makemeahanzi / hanzi-writer-data 派生自 Arphic 1999 公共授权字体，分发前必须复核授权义务；本地非分发实验不受此阻塞 |
| 真机手感未验证 | Simulator 只证明可运行；Apple Pencil 观感需真机评审 |

已接受但受限：项目名、仓库名、占位 bundle ID、本地 persona 只限私有非分发面，不代表商业授权。

## 模型与 Key 边界（2026-07-23 核对官方说明）

- Key 按区域区分；部分区域（北京、新加坡、东京、法兰克福等）调用需把 Workspace ID 放进 Base URL，接入时以实际账号页面为准。
- 移动端正式调用经安全后端；官方建议由后端为不可信环境签发临时 Key（默认 60 秒，可配 1–1800 秒，继承签发 Key 权限）。
- 参考：[Qwen 首次 API 调用](https://help.aliyun.com/en/model-studio/first-api-call-to-qwen)、[临时 API Key](https://help.aliyun.com/en/model-studio/application-obtain-temporary-authentication-token)。
- 未核实：模型实时可用性/配额/价格、大陆到端点的可达性与延迟、供应商对请求/日志/备份/训练/删除的条款、"数据不出境"的合同与全链路证据、自部署版本许可证与运维责任。

## 待用户决定 / 待用户执行

1. **版式：用户在哪写、魂在哪回应？（现在是最大的阻塞项）** 现在两者共用整页、毫无分隔，实测会互相压字。候选：顺序往下排 / 上下分区 / 魂接在用户写完那行的下面。它同时卡住三件事：回应落在哪、成页预告那团墨画在哪（现在临时放左上角）、以及成页时怎么只识别「这一轮写的」而不是整页。E6 之前必须有结论。
2. **真机验证（最要紧）**：iPad 10 支持 iPadOS 27。装上后要验三件——中文手写识别是否可用（决定整条输入端）、`TimelineView` 连续动画是否正常（决定逐笔重播能不能做）、真实书写的停顿分布（决定 E3c 的成页阈值）。三件在模拟器里都验不了。
3. **画布输入策略**：`InteractionSettings.drawingPolicy` 暂取 `.anyInput`（手指也能写）。要不要只认 Apple Pencil？只认笔的话按 AGENTS.md 必须明确告知，不能让用户对着无反应的纸猜。
4. 行距 1.6 倍字高、页边距 8% 短边、用户笔宽 0.5 mm，都是量级推算，真机观感待复核。
4. 正式 bundle ID、签名主体、原创公开品牌与 Persona 方向；何时购买 Apple Developer Program（beta 期间无法分发，正式版约九月）。
5. `AGENTS.md` 被删除的发布前安全清单（自残危机兜底、AI 披露等）——用户已明确不恢复，此处仅留记录以免将来误以为遗漏。

## 用户偏好

- 中文交流，大白话解释术语，需要类比与详细但易懂的说明。
- 区分「已验证事实 / 合理假设 / 待用户决定」，不混写。
- 不替用户拍板命名、bundle ID、品牌、付费与架构方向。
- 不静默兜底：阻塞、测试失败、远端分叉必须暴露。
- 用户会明确要求「先不要进行下一步」，必须停住等确认。
- 重视真实可演示，不接受把命令 exit 0 当成体验通过。
- 每次实质任务收口自动执行安全 Git 同步。

## Agent 自身的判断错误（记下来避免重犯）

- **2026-08-27：把「模拟器返回了什么」当成「平台支持什么」。** 只依据模拟器上 `supportedLanguages` 返回 20 种拉丁语言，就断言 PencilKit 不支持中文，还据此回滚了最低系统。用户翻出官方 session 的支持语言列表才纠正。教训：运行时能力查询反映的是**当前环境装了什么**，不是平台契约；平台契约要看官方文档。两者不一致时先找原因，不要拿环境读数推翻文档。
- 同一错误的第二个表现：结论下成了「输入端整体换路」，而正确结论应是「拉丁字母照用，只有中文这一格待验证」。能力缺失要按语言/按功能定位到最小范围，不要放大成整条路线报废。

## 已知陷阱

- **模拟器空闲时冻结渲染循环**（2026-08-27 实测）：`TimelineView` 的 `.animation` 与 `.periodic` 两种调度在模拟器里都只出一两帧就停，画面卡在第一个字。由 `@State` 变化触发的重绘照常生效。因此 E8 的逐字显现改成异步循环推进状态。**连带后果：`HandwritingReplayView` 的逐笔重播用的是 `TimelineView`，它的连续动画在模拟器里无法验证，必须真机确认。** 不要因为模拟器里不动就以为代码错了——这和中文识别模型缺失是同一类环境限制。

- **两个配置常量之间有隐藏耦合**（2026-08-28 被测试撞出来，不是事先想到的）：成页倒计时每 0.1 秒轮询一次，而最短的预告期是 `shortestWait × hintFraction`。后者一旦短于前者，一次轮询就跨过整个预告期，「可撤销的预告」直接消失，症状只是「提示时有时无」，不报错也没人会想到去查那个间隔。已加断言 `PageCommitTriggerTests.testHintWindowOutlastsTheCheckInterval` 守住。
- **`@Observable` 不比较新旧值**（2026-08-29 踩到，代价是一个真 bug）：把同一个值再写一遍，观察者照样收到通知、界面照样重建。倒计时每 0.1 秒写一次 `phase`（大多数次值没变），界面每秒重建十次，PencilKit 的墨迹层被反复标记为需要重绘却一直没重绘——**用户写的字从纸上消失了**，而数据全在、纸色也在。凡是被高频调用的地方赋值给 `@Observable` 属性，都要先比较。
- Swift 6 下**不能**给 `XCTestCase` 子类整体标 `@MainActor`（与继承来的 nonisolated 初始化方法冲突，直接编译报错）。要测 `@MainActor` 的类型时，隔离标在具体测试方法上。
- Xcode 26.6 并行 test clone 触发 `DVTiPhoneSimulator` assertion → 测试关闭并行。
- Xcode 曾自动建嵌套 Git，已可恢复地移到 `.git/xcode-nested-repo-backup-8d5eca1`。
- 工程用 objectVersion 77 同步文件组：`Sources/`、`Tests/` 下新增文件自动入 target，不需手改 pbxproj。
- kiro-pet 已迁至 `110_KiroPet`，不得放回本仓库。

## 历史（精简）

- **2026-07-15 至 08-26（起步与第一次深度审阅）**：仓库骨架、规则/记忆/脚本/Config、GitHub 私有远端、Xcode 工程纳入根仓库、修正为 iPadOS/仅 iPad、建立 `AGENTS.md`；完成又删除了 Tasks 2–4 与 Magic Stroke Lab（`e08f4c3`）。写入强制注释规则、删除位图路线、文档收敛为三份、目录拉平。全仓库审阅确认最大缺口是 Oracle 与引擎间无桥、参数无单位契约、手绘感四大机械来源仍在；用户拍板首个产品目标为「手写文字进 → 手写文字出」。
- **2026-08-27**：建立单位契约与集中配置（D1/D2，只有字高用毫米、其余全是比例）；D3 时长不变量落进类型、D4 改单调时钟、D6 生长几何提成 `StrokeGrowth` 并补测。跑 F3 门禁时误把「模拟器缺中文模型」判成「平台不支持中文」并据此回滚了 iOS 27，经用户以官方文档纠正后已更正并恢复；查清 Arphic 1999 与 2010 只差商用许可，以及 iOS 内置无任何中文楷体或手写体。
- **2026-08-27（计划 H 清理，`d0f39f1` → 合并进 `main` `d5641c8`）**：删 Lab 诊断面 4 个文件与其中 10 个手感参数、`/5` 魔数、全部布局颜色魔数；渲染层搬家瘦身为 `HandwritingReplayView` 并删掉自动缩放；删 `ContentView` 空转中转层；新建 `PageAppearance` 收敛纸墨色；A9（写死 60Hz）一并做掉。清死配置：pbxproj 的 iPhone 方向设置、门禁脚本 7 个失效排除项、`.gitignore` Python 条目、Secrets 模板图像 key。文档收敛为四段管道 + A–H 计划。删陈旧分支 `chore/pre-development-readiness`（删前确认是 main 祖先）。更正一个错误印象：**Xcode 26.6 完全能用 PencilKit**，只有「手写识别」这一个新 API 要 iPadOS 27，因此 E3/E2/E8/D/A 都不必等升级，执行顺序已重排。
- **2026-08-27（E2 + E8）**：输出侧脚手架跑通。打包寒蝉手拙体 v2.500；`Handwriting/TextLayout` 用 CoreText 断行定位（该字体连汉字都不等宽，按格子排会挤散，中文换行规则自己写必然漏）；`HandwrittenTextView` 逐字显现。修掉自己两个实现问题：排版失败返回 nil 导致纸上空白无提示（正是自己要防的静默兜底，改成显式三态）、基线把降部算进去导致字偏高（改由排版层输出 ascent）。踩到环境限制：模拟器空闲冻结渲染循环，`TimelineView` 两种调度都停帧，改用状态驱动。54 测试通过。
- **2026-08-28（F4 + F5 + E3a + E3b）**：升 Swift 6 语言模式 + 门禁加纯逻辑层 import 白名单（已反向验证会红字失败）。F4 的价值当场应验：Swift 6 直接挑出 `PencilStrokeReader` 不该绑主线程，Swift 5 下只会是警告。接上 PencilKit 画布（关掉滚动缩放，画布坐标即页面坐标）与 `PencilStrokeReader`（用框架的 `.distance` 等距采样，不自己按参数值均分）——引擎自此有真实输入源。压感刻意不伪造：force 量程未知且不公开，只如实报告有无。用户实测确认能写字、44 笔 2610 采样点合理、force 精确为 0。据此把示例回应从运行界面挪进 Preview。64 测试通过。
- **2026-08-28（E4 接线 + E1a/E1b）**：`HandwritingRecognizer` 包装 `PKStrokeRecognizer`，把「请求的语言」与「本机能用的语言」的差异变成显式数据。当场撞出一个重要事实：**缺语言模型时识别器返回垃圾而不是 nil**（写「你好」得到 `15.47`），而且无法过滤——没有中文模型就无从判断「这几笔本来是中文」。然后做 E1：`graphics.txt` 30.78 MB 只留 medians 扁平化成 5.26 MB（9574 字，坐标 1024 em、y 轴朝上），`GlyphStrokeProvider` 查表、`GlyphStrokeLayout` 按字面方格等宽排版。刻意**不复用 `TextLayout`**：字体是比例字宽、字形数据是等宽方格，混用会让字互相压。汉字自此能由引擎真正逐笔画出。
- **2026-08-28（E3c + E3d）**：成页触发与落笔中断。最重要的一条是**阈值不再是固定秒数**——改成本页停顿中位数 × 倍数，因为实测证明书写本身 76% 的时间在停顿，固定秒数必然误触发（原「2.8 秒」就是如此）。四个信号按主次接好：长停顿为主，终止标点按比例加速，等待期后段渗墨预告（此刻再写一笔即取消，用渗墨而不是进度条是因为纸上不许有控件），Pencil 悬停只做**有界**延长且硬件不支持时不做任何补偿。新建 `PageCommitTrigger`（纯判断，可单测）与 `DiaryPageModel`（`@Observable`，持有轮询循环）——把循环从视图里搬出来是因为视图结构体捕获的状态可能过期，那类错误不报错只偶发判断错。E3d 把渲染层入参从 `Instant?` 改成 `ReplayPlayback` 三态：原契约根本表达不了「停在当时的进度」，只能被迫映射成「画完」，正好把「你打断了它」这个信息抹掉。打断点用落笔（`canvasViewDidBeginUsingTool`）而不是这一笔写完——你正在写的时候纸不该有自己的动作。过程中被测试撞出一个隐藏耦合（轮询间隔会吞掉过短的预告期），已加断言守住。顺手清掉 README 状态表里三处自相矛盾的旧行。124 测试通过。
- **2026-08-29（修 bug + E3e + E4b + A1）**：先修自己上一轮造的 bug——用户写的笔画写完就从纸上消失。根因是倒计时每 0.1 秒往 `phase` 写值且值没变也写，`@Observable` 照样发通知，界面每秒重建十次，PencilKit 墨迹层被反复标脏却因模拟器空闲冻结渲染循环而没重绘。改成只在阶段真变化时发布 + 阶段不携带高频数据，一轮从几十次发布降到 4 次，加回归守卫。顺带按用户实测把成页预告从产品面撤下（「看上去真的很像个 bug」：淡、小、在角落、离书写处半页远、只存在一秒半），改成 DEBUG 横幅；需求没撤，正确形态等版式决定。然后 E3e（按落笔时刻切轮次，只把新写的交给识别器）——同时纠正了我此前「这取决于版式决定」的错判；E4b（落笔前就查本机识别能力并如实告知，语言名走系统本地化不写字面量），过程中发现工程 `developmentRegion` 是 `en` 导致语言名显示成 "Chinese"，改成 `zh-Hans` 让现状自洽，并记下「没有本地化文件」这个真实缺口。最后 A1（抖动改成沿弧长的连续摆动，位移打法线方向；波长由手抖频率 10 Hz 与书写速度推出而不是拍一个数；幅度顺势从 0.018 提到 0.03，因为原值小只是为了掩盖白噪声难看）。`StrokeWobbleTests` 把「不是白噪声」变成可断言的量（相邻偏移的平均变化量，白噪声理论值 1.13σ，实现须低于 0.35σ）。144 测试通过。
