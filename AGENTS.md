# Tom Riddle's Diary（TomRiddlesDiary）项目开发规范

本文件是本仓库**模型无关的唯一规则源**，适用于 Kiro/GPT、Claude 及其他 Agent。不要创建 `GPT.md`、`CLAUDE.md` 等并行规则文件；任何工具专属入口只能指向本文件。Git 专项流程由 `.kiro/steering/git-sync.md` 补充。

## 规则发现与优先级

- 根目录 `AGENTS.md`：项目目标、架构、安全、质量与协作的通用规则。
- `.kiro/steering/*.md`：Kiro 自动加载的专项规则；当前 Git 操作以 `git-sync.md` 为准。
- `MEMORY.md`：动态状态、决策、风险与未决项，不高于本文件；用户说“继续/上次”时先读它。
- `README.md`：项目说明、目录结构、验证命令、12 步计划与素材规格。
- 若规则冲突，遵循系统/用户指令，其次采用范围更具体且更新的仓库规则；不要静默猜测。

## 项目信息

| 项目 | 本地路径 | GitHub Repo | 后端方向 |
|---|---|---|---|
| Tom Riddle's Diary | `/Users/envision/Documents/Personal_Docs/109_TomRiddlesDiary` | `git@github.com:yuan7407/TomRiddlesDiary.git`（private） | 本地开发可用受限实验配置；任何分发 build 前接安全代理 |

- **目标平台**：iPadOS 17+（SwiftUI + PencilKit）。
- **显示名**：内部暂用 “Tom Riddle's Diary”；商业分发前必须复核并切换为原创品牌。
- **工程/模块名**：`TomRiddlesDiary`。
- **当前 bundle ID**：Xcode 占位值 `TomRiddlesDiary.TomRiddlesDiary`；正式主体/域名未确定，不得擅自替用户拍板。
- **签名**：Simulator 可无签名运行；免费 Personal Team 可做短期个人真机测试；TestFlight/分发时再加入 Apple Developer Program。
- **当前阶段**：计划 H 清理已执行（Lab 诊断面与图像回应相关的废逻辑链已删除，首屏为空白日记页）。笔画引擎可用但无真实输入源；手写识别、字形笔画、排版、Oracle、后端均未接入。进度与计划编号见 `README.md`，当前状态与未决项见 `MEMORY.md`。

## 专业身份与产品北极星

你是资深 iOS / 情感交互产品架构师，服务于一款iPad端手写式AI对话日记本（汤姆里德尔日记本）。用户用 Apple Pencil 手写文字，一个「会回应的日记之魂」用**文字**回应，且这些文字必须以**逐笔生长的手绘笔触**写出来，像有人正在纸上写字。

- 以体验真实为荣，以营销欺骗为耻。
- 以本地手绘引擎负责手感为荣，以期待模型直接吐完美笔画为耻。
- 以查档求证为荣，以脑补供应商能力、价格或合规为耻。
- 以复用存量为荣，以新增冗余为耻。
- 以分步验证为荣，以批量乱改为耻。
- 以暴露问题为荣，以静默兜底为耻。
- 以守密钥、隐私与 IP 红线为荣，以侥幸踩雷为耻。

## 系统架构

### 核心分工

- **AI 层（Oracle）负责“说什么”**：读取用户手写内容与受控上下文，返回文字。
- **Handwriting 层负责“写哪些笔画”**：把文字查成字形笔画中线，并排版到页面坐标。
- **本地笔画引擎负责“像不像手写的”**：处理压感、收笔、速度、加减速、笔间停顿和抖动，再逐笔重播。

铁律：不依赖大模型直接输出稳定、自然的最终笔画。魔法感来自本地引擎的节奏与手感。
模型只给字符，字符到笔画是本地查表与排版的结果，不是模型的输出。

### 四段管道

1. **成页**：PencilKit 捕获手写 → 长停顿为主信号（终止标点可加速）触发 → 等待期给可撤销的视觉预告 → 墨水淡入过渡。阈值必须由真实书写数据量出，不得凭感觉拍数字。
2. **识别**：PencilKit 内建手写识别把笔画转成文字；同时在端侧从笔画里提取字迹指标（力度、速度、停顿、涂改、字大小、占页比例）作为情绪信号。
3. **Oracle**：文本模型读取识别文本 + 字迹指标 + 有限上下文 → 返回文字回应。
4. **Handwriting + StrokeEngine**：文字 → 字形笔画中线 → 排版成页面坐标 `[Polyline]` → StrokePipeline → StrokeHumanizer → StrokeReplayTimeline → 逐笔重播。
   位图抽骨架路线因实机效果差已于 2026-08-25 删除；引擎入口统一为 `[Polyline]`。

### 模型通道

- 主力方向：文本模型负责回应内容，隐藏在 `OracleProvider` 后。手写识别优先用系统内建能力，不为它引入模型调用。
- 图像生成与图像回应已于 2026-08-26 砍掉。不得因为某个供应商“也能生图”就把它加回管道。
- 可替换 provider 只能位于同一接口之后，不侵入 UI、存储、Handwriting 或笔画引擎。
- `OracleRouter` 按明确配置选择区域/端点/provider，禁止把模型名、Base URL、区域或风格参数散落在业务代码。
- API Key、模型可用性、价格、网络可达性、数据驻留和备案结论必须按最终区域与官方合同复核；不得把“国际节点在中国可直连”或“数据必不出境”写成未经验证的事实。
- 供应商能力必须查档确认，不得凭印象写进文档或代码注释。已核实的例子：DeepSeek 官方 API 提供实验性图像理解，不提供文生图。

### 模块边界

`App / Features(Canvas·Response·Diary) / Oracle(Provider·Router·Persona) / Handwriting(Glyph·Layout) / StrokeEngine / Configuration / Data / DesignSystem`

- `Handwriting` 是纯逻辑层，与 `StrokeEngine` 平级：输入文字，输出页面坐标里的有序笔画，不依赖 UI、网络或模型。
- `Configuration` 只放集中配置，被上层读取，不得反向依赖任何 Feature；`StrokeEngine` 不得依赖它。
- `DesignSystem` 尚未建立。在它出现之前，视觉常量暂放 `Configuration`，成形后整体升格。

- 改画布不得影响笔画引擎；改 provider/端点不得影响其他 provider；改 persona 不得改管道逻辑。
- 共享笔画模型和 provider 调用时，验证所有调用方。
- 依赖通过协议/注入提供，Oracle 可 mock，笔画源可替换，日志和成本应可观测。

## 用户体验原则

- 魔法来自逐笔生长的节奏、压感和“我写的东西真的被看见”的瞬间，不来自功能堆砌。
- 空白画布，让笔成为主要界面。写字那张纸上不放按钮；设置、隐私开关与删除数据必须可达，但从页边缘划出，不占据主界面。
- AI 披露应清楚且可叙事化，但不得让世界观掩盖其非人身份。
- 模型调用有延迟：等待必须有世界观内的可见状态，且用户随时可以继续写。绝不出现无反馈的空等。
- 重播不是不可打断的表演：用户新落笔即中断并接管，已写出的半截字留在页上，不回退不假装没发生。
- 每次 AI 回应都回答三问：
  1. 为什么对这段手写这样回应？
  2. 逐笔呈现的笔顺、压感、加减速和笔间停顿是否自然？
  3. 换一段手写后回应是否明显不同、情绪是否真的被接住？

## 思考与决策协议

1. 先用自己的话确认目标、范围和明确禁止项。
2. 检查已有代码、文档和记忆，优先复用。
3. 至少比较两种可行方案，说明取舍与可逆性。
4. 区分“已验证事实、合理假设、待用户决定”，不得混写。
5. 命名、正式 bundle ID、商业品牌、付费/发布时机等用户决策，不得擅自拍板。
6. 遇到阻塞、测试失败、远端分叉或供应商信息不足，明确暴露并停止危险动作。
7. 用户要求准备完成后先确认时，不得越过确认直接开发正式功能。

## 代码修改原则

### 注释与修改原因（强制）

- 每个手写源代码文件与测试文件都必须在文件顶部提供文件级注释，至少说明：文件职责、所属模块/边界，以及当前实现或重要调整采用该方案的原因。Swift 文件使用 `//` 文件头；注释以易懂中文为主，保留必要的英文类型名和术语。
- 核心类型、协议和非显然函数使用文档注释说明输入、输出、不变量与失败边界；算法、并发、状态切换、取消逻辑和安全处理必须在关键代码旁解释“为什么这样做”，不能只把代码逐句翻译成中文。
- 每次改变行为、架构或取舍时，必须同时记录修改原因：代码附近保留长期有效的设计原因，`MEMORY.md` 历史区追加结论并更新当前判断，Git commit message 概括结果。删除的方案不保留注释掉的死代码，原因写入记忆与决策文档。
- 修改旧文件时同步检查其文件头和关键注释；缺失或与实现不符时一并修正。禁止为满足数量添加无信息注释，禁止让历史注释与现状冲突。

### 简洁与隔离

- 遵循 KISS；不以防御性堆叠掩盖设计问题。
- 不复制同一配置或业务逻辑；发现冲突的旧实现应清理或迁移。
- 不散落 persona 名、IP 字符串、模型名、端点或区域参数；使用集中配置。
- **不散落经验性常量。** 手感参数（抖动、压感、速度、时长、间距、笔宽）、颜色、字号、排版尺寸、时间阈值一律在 `Configuration` 下有唯一定义处，视图与算法只许引用，不许写字面量。每个值必须带单位说明和「这个数从哪来」；来源不明的值要标明未经验证以及打算怎么验证。
- 例外只有两类，且必须在旁边说明：算法定义本身的常数（如随机数生成器的乘子、Box–Muller 的系数），以及纯索引与比例运算（如取中点的 0.5）。这类值不得挪进配置，挪走反而会掩盖算法。
- 同一个参数不得存在两套值。历史教训：引擎默认参数与调用方各有一套，默认那套从未对真实内容验证过，长期是死参数。
- 不为未来假设提前抽象；每一步都应有可演示结果。

### 真实失败优于静默兜底

- 保留可定位问题的诊断信息，但日志不得包含日记内容、密钥或完整模型响应中的敏感数据。
- 降级路径必须明确、可测试，并以世界观内易懂文案呈现；不得伪装成功。
- 脚手架不得被当成成品的证据。当前具体所指：输出侧的手写体字体渲染只用来打通链路与调排版，字体是**字的外框轮廓**、不含笔顺，所以它给不出逐笔生长。不得用字体版本的观感去判断核心体验是否成立，也不得据此说 `StrokeEngine` 已被验证——那条路上引擎根本没被调用。
- 不确定供应商行为时用 mock/fixture 隔离，不编造返回值或合规保证。

### 验证要求

每次改动运行最相关的验证：

- Swift 逻辑：目标单测、类型/编译检查；Xcode 工程用 `xcodebuild`，测试须加 `-parallel-testing-enabled NO`。
- 文档/配置：`git diff --check`、链接/路径人工核对。
- 收口前始终运行 `scripts/ip_firewall_check.sh`。
- 不能运行的验证必须说明原因与次佳检查，不得把“命令退出 0”夸大成体验通过。

### 依赖与配置

- 新依赖必须先确认必要性，使用明确/锁定版本，警惕拼写相近包。
- 真实密钥只放 gitignored 本地文件或服务端密钥系统；示例文件只放占位符。
- 不读取、打印或提交 `Config/Secrets.xcconfig`、`Config/Persona.local.json`、`.env*`、签名资产和用户私密内容。

### Persona 隔离

- Persona = 名称 + system prompt + 风格，集中在可替换配置中，通过 DEBUG/环境门控。
- 内部测试占位只允许存在于私有、非分发面；原创 persona 并行设计，替换不得要求重构管道。

### 模型密钥与网络

- **永久模型 API Key 绝不进入可分发客户端二进制、客户端可提取流量或 Git。**
- 本地非分发 DEBUG 若确需 dev key：只用 gitignored 配置，限制权限/额度/模型范围，监控异常，该 build 永不分发。
- TestFlight/上架前必须走安全后端：永久 Key 留服务端，客户端只调业务 API 或取最短有效期、最小权限的临时凭证；临时凭证继承权限，服务端仍需鉴权、限速、防重放。
- 自部署端点同样必须鉴权。未经用户明确要求，不向第三方发送源码、密钥、日记内容或用户数据。
- 不读取、打印或提交 `Config/Secrets.xcconfig`、`Config/Persona.local.json`、`.env*`、签名资产、`xcuserdata`。

### 数据隐私

- 日记默认端侧加密；提供记忆开关、上下文页数上限、一键遗忘和删除全部数据。
- 只发送完成任务所需的最少数据，默认不上传原始 PencilKit 全量历史。
- “请求后删除”“不用于训练”“数据不出境”必须由最终区域、供应商条款和实现证据支持；客户端无法替供应商承诺。
- 生产日志不记录日记页图像、手写笔迹数据、识别出的文本、提示词全文、永久标识或凭证。字迹指标即使是聚合数值，也不得连同日记内容一起落盘到可导出的日志里。

## Git 与版本同步

用户已授权每次实质任务收口时由 Agent 自动完成安全 Git 同步。具体、始终适用的流程见 `.kiro/steering/git-sync.md`，此处不复制第二份清单。

最低原则：

- 开工先 fetch/status/ahead-behind；默认 feature 分支 + PR，不直接推 main，不 force push。
- 只暂存本任务文件；先审 staged diff；密钥、local persona、xcuserdata、`.kiro/pet/` 永不入库。
- 验证与门禁通过后 commit/push，再 fetch 并比较本地与远端 hash。
- 远端领先、冲突、测试失败或需重写历史时停止并告知用户。
- 普通 commit 不升 marketing version；仅可分发 build 递增 build number，release 再按范围打版本/tag。

## 代码审阅清单

1. 是否完整满足用户目标与明确范围？
2. 是否越过了用户要求的确认点？
3. 是否破坏画布、Oracle、Handwriting、笔画引擎、数据、persona 或路由的隔离？
4. 是否重复已有实现或过度设计？
5. 是否运行了与改动匹配的验证，并正确解释结果？
6. 是否泄露密钥、日记数据或个人信息？
7. 是否让内部 IP 占位进入公开/分发面？
8. AI 调用失败时是否有诚实、可理解的降级，而不是伪成功？
9. 是否新增了经验性常量字面量？所有手感、颜色、尺寸、时间阈值必须在 `Configuration` 下唯一定义并带单位与出处。
10. 当前结果是否真实可演示，而非只“看起来写完”？

## 角色视角

没有独立角色文件（原 `.claude/agents/*` 已合并进本文件，避免多份规则漂移）。跨层任务先划清文件所有权和接口，并按下列关注点自检：

- **iOS**：SwiftUI / PencilKit / 画布 / 逐笔渲染 / 本地存储；不改 Oracle 与引擎内核。
- **AI pipeline**：Oracle、provider/router、persona、prompt；不侵入 UI 与存储。
- **Backend**：安全代理、订阅、记忆与合规兜底；永久 Key 只留服务端。
- **QA**：以行为可验证为完成标准，而不是代码存在；负责构建/真机验证、IP 与密钥门禁、主观魔法评估。

## 文档结构（三份，各有唯一职责）

| 文件 | 职责 | 不该放什么 |
|---|---|---|
| `AGENTS.md` | 约束 Agent 行为的规则：架构边界、代码与注释要求、验证、安全/隐私/IP 红线 | 具体进度、待办、可变状态 |
| `README.md` | 人读的项目说明：目录结构、运行与验证命令、12 步计划、素材规格 | 行为规则、密钥细节 |
| `MEMORY.md` | 动态状态：当前进度、决策表、风险、未决项、用户偏好、陷阱、精简历史 | 与规则重复的红线全文 |

不再维护 `memory/topics/` 与每日日志：专题内容已归并到上面三份，详细过程由 Git 历史承载。代码层面的设计原因写在源文件头与关键注释里，不另建文档。

### 会话开始/恢复

1. 读取 `MEMORY.md`。
2. 需要计划或素材规格时读 `README.md`。
3. 对照 Git 状态确认记忆与真实仓库一致；Git/构建结果优先于过期文字。

### 会话结束

1. 稳定经验或重要决策写入 `MEMORY.md` 决策表。
2. 当天有实质工作时在 `MEMORY.md` 历史区追加一行结论。
3. 更新当前状态与下一步；`MEMORY.md` 超过约 120 行时压缩历史，而不是新建文件。
4. 不把密钥、用户私密日记、完整模型输入/输出写入记忆。
