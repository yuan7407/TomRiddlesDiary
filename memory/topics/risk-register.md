# 风险登记与缓解

| 风险 | 处置 / 状态 |
|---|---|
| 魔法体验不动人（生死项） | Task 1A 机械预检已完成；Task 1B 真实模型预检 + 第 8 步 App 内完整评审仍待做 |
| 把夹具误称为 Qwen/情绪证据 | manifest、spike README、根 README 均明确限制；报告时只称机械 smoke test |
| 图像生成延迟/成本高 | 文字/离线反馈先出现、图像并行；真实价格和延迟在选区后测量 |
| 永久 Key 泄露 | 不进入可分发客户端/Git；本地非分发 DEBUG 例外需最小权限/额度/监控；TestFlight 前安全后端 |
| 临时凭证权限过大 | 最短 TTL、最小权限、服务端鉴权/限速/防滥用；注意其继承签发 Key 权限 |
| 未经证据承诺“数据不出境/读完即删” | 设计目标与供应商事实分开；按最终区域、合同、日志/备份和实现逐项验证 |
| 内部 IP 占位进入公开面 | 门禁 + staged diff 人工审查；正式 bundle ID/品牌在注册与分发前原创化 |
| 单一模型/区域依赖 | `OracleProvider` / `OracleRouter` 隔离；自部署需另行核对许可证、成本和运维 |
| Xcode/签名阻塞 | Simulator build 已通过；真机可先用免费 Personal Team，TestFlight 时再付费 |
| 文档规则漂移 | `AGENTS.md` 单一事实源；`CLAUDE.md` 只指针；专项 steering 不复制全文 |
| 门禁扫描超时/误报 | 改用 Git tracked + non-ignored 文本清单，跳过二进制、`.venv`、构建产物与内部策略；已复跑通过 |
| 成人情感产品合规 | AI 披露、年龄、危机兜底、删除数据、内容标识与目标市场要求在发布前逐项验证 |

## 当前已接受但受限的内部风险

- 项目名、仓库名、占位 bundle ID 和本地 persona 只限私有非分发面，不代表获得商业授权。
- 本地 DEBUG 模型直连若未来启用，只限个人受控实验；相关 build 永不分发。
